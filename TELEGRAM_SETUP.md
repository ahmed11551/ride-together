# 🤖 Настройка Telegram Mini App

Это руководство поможет вам настроить Telegram Mini App для вашего приложения Ride Together.

## 📋 Что такое Telegram Mini App?

Telegram Mini App - это веб-приложение, которое запускается внутри Telegram. Пользователи могут использовать ваше приложение прямо в Telegram, без необходимости устанавливать отдельное приложение.

## 🚀 Быстрый старт

### 1. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Ride Together`)
   - Введите username бота (должен заканчиваться на `bot`, например: `ride_together_bot`)
4. Сохраните токен бота (понадобится позже)

### 2. Настройка Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Введите название Mini App (например: `Ride Together`)
5. Введите описание (например: `Платформа для совместных поездок`)
6. Загрузите иконку (512x512px, PNG)
7. Введите URL вашего приложения:
   ```
   https://your-vercel-app.vercel.app
   ```
   Или для разработки:
   ```
   https://your-ngrok-url.ngrok.io
   ```

### 3. Настройка переменных окружения

Добавьте в Vercel (или `.env` для локальной разработки):

```env
VITE_TELEGRAM_BOT_TOKEN=your-bot-token-here
```

⚠️ **Важно:** Не публикуйте токен бота в публичных репозиториях!

### 4. Установка зависимостей

```bash
npm install @twa-dev/sdk
```

## 🔧 Интеграция в приложение

### Структура файлов

```
src/
├── contexts/
│   └── TelegramContext.tsx      # Контекст для работы с Telegram Web App API
├── hooks/
│   └── useTelegramAuth.ts        # Хук для авторизации через Telegram
└── components/
    └── telegram/
        ├── TelegramBackButton.tsx    # Кнопка "Назад"
        └── TelegramMainButton.tsx   # Главная кнопка
```

### Использование

#### 1. Проверка, запущено ли приложение в Telegram

```tsx
import { useTelegram } from '@/contexts/TelegramContext';

function MyComponent() {
  const { isTelegram, user, webApp } = useTelegram();

  if (isTelegram) {
    // Приложение запущено в Telegram
    console.log('Telegram user:', user);
  }
}
```

#### 2. Использование кнопки "Назад"

```tsx
import { TelegramBackButton } from '@/components/telegram/TelegramBackButton';

function MyPage() {
  return (
    <div>
      <TelegramBackButton fallbackPath="/" />
      {/* Ваш контент */}
    </div>
  );
}
```

#### 3. Использование главной кнопки

```tsx
import { TelegramMainButton } from '@/components/telegram/TelegramMainButton';

function MyForm() {
  const handleSubmit = () => {
    // Обработка отправки формы
  };

  return (
    <div>
      {/* Ваша форма */}
      <TelegramMainButton
        text="Отправить"
        onClick={handleSubmit}
        disabled={false}
      />
    </div>
  );
}
```

## 🔐 Авторизация через Telegram

Приложение автоматически определяет пользователя Telegram и может использовать его данные для авторизации.

### Настройка базы данных

Добавьте поля для Telegram в таблицу `profiles`:

```sql
ALTER TABLE profiles
ADD COLUMN telegram_id TEXT UNIQUE,
ADD COLUMN telegram_username TEXT,
ADD COLUMN telegram_first_name TEXT,
ADD COLUMN telegram_last_name TEXT,
ADD COLUMN telegram_photo_url TEXT;
```

### Использование

Хук `useTelegramAuth` автоматически обрабатывает авторизацию:

```tsx
import { useTelegramAuth } from '@/hooks/useTelegramAuth';

function MyComponent() {
  const { telegramUser, isTelegram } = useTelegramAuth();
  
  if (telegramUser) {
    console.log('Telegram user:', telegramUser);
  }
}
```

## 🎨 Адаптация UI для Telegram

### CSS переменные Telegram

Приложение автоматически применяет тему Telegram через CSS переменные:

```css
--tg-theme-bg-color          /* Фон приложения */
--tg-theme-text-color       /* Цвет текста */
--tg-theme-hint-color       /* Цвет подсказок */
--tg-theme-link-color       /* Цвет ссылок */
--tg-theme-button-color      /* Цвет кнопок */
--tg-theme-button-text-color /* Цвет текста кнопок */
```

### Адаптация для мобильных устройств

Telegram Mini App работает на мобильных устройствах. Убедитесь, что ваш UI адаптирован:

- Используйте `safe-area-inset-*` для учета вырезов экрана
- Оптимизируйте размеры кнопок для касаний
- Используйте полноэкранные модальные окна

## 🧪 Тестирование

### Локальное тестирование

1. Используйте [ngrok](https://ngrok.com/) для создания публичного URL:
   ```bash
   ngrok http 8080
   ```

2. Используйте этот URL в настройках Mini App в BotFather

3. Откройте бота в Telegram и запустите Mini App

### Тестирование на продакшене

1. Задеплойте приложение на Vercel
2. Обновите URL Mini App в BotFather на URL Vercel
3. Откройте бота в Telegram и протестируйте

## 📱 Особенности Telegram Mini App

### Доступные функции

- ✅ Автоматическое определение пользователя
- ✅ Кнопка "Назад" (BackButton)
- ✅ Главная кнопка (MainButton)
- ✅ Тактильная обратная связь (HapticFeedback)
- ✅ Тема приложения (светлая/темная)
- ✅ Viewport управление
- ✅ Закрытие приложения
- ✅ Открытие ссылок
- ✅ Показ popup/alert/confirm

### Ограничения

- ❌ Нет доступа к файловой системе
- ❌ Нет доступа к камере (только через API)
- ❌ Нет доступа к геолокации (только через API)
- ❌ Ограниченный доступ к localStorage

## 🔒 Безопасность

### Проверка данных от Telegram

Всегда проверяйте данные, полученные от Telegram, используя хеш:

```typescript
import { validateTelegramWebAppData } from '@/lib/telegram-security';

const isValid = validateTelegramWebAppData(initData, botToken);
if (!isValid) {
  // Данные невалидны
}
```

⚠️ **Важно:** Никогда не доверяйте данным от клиента без проверки на сервере!

## 📚 Дополнительные ресурсы

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [@twa-dev/sdk Documentation](https://github.com/twa-dev/sdk)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## 🐛 Решение проблем

### Mini App не открывается

1. Проверьте URL в BotFather
2. Убедитесь, что URL доступен публично (не localhost)
3. Проверьте, что приложение задеплоено

### Данные пользователя не загружаются

1. Убедитесь, что приложение запущено в Telegram (не в браузере)
2. Проверьте, что `TelegramProvider` обернут вокруг приложения
3. Проверьте консоль на наличие ошибок

### Кнопки не работают

1. Убедитесь, что используете компоненты из `@/components/telegram`
2. Проверьте, что `isTelegram === true`
3. Убедитесь, что `webApp` не null

---

**Готово!** Теперь ваше приложение готово работать как Telegram Mini App! 🎉

