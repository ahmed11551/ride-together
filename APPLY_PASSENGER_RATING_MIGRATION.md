# 🚀 Применение миграции рейтинга пассажира

## ⚡ Быстрый способ (3 шага)

### Шаг 1: Откройте Supabase Dashboard
👉 [https://app.supabase.com](https://app.supabase.com) → Ваш проект

### Шаг 2: Откройте SQL Editor
👉 Левое меню → **SQL Editor** → **New query**

### Шаг 3: Скопируйте и выполните
1. Откройте файл: `supabase/migrations/20250131000001_add_passenger_rating.sql`
2. Скопируйте **весь** содержимое (Ctrl+A, Ctrl+C)
3. Вставьте в SQL Editor
4. Нажмите **Run** (или `Cmd+Enter` / `Ctrl+Enter`)

---

## ✅ Проверка успешного применения

После выполнения выполните этот SQL для проверки:

```sql
-- 1. Проверка поля passenger_rating
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'profiles'
AND column_name = 'passenger_rating';

-- 2. Проверка функций
SELECT proname 
FROM pg_proc
WHERE proname IN ('update_passenger_rating', 'update_driver_rating', 'update_user_ratings');

-- 3. Проверка триггера
SELECT tgname 
FROM pg_trigger
WHERE tgname = 'trigger_update_ratings_on_review';

-- 4. Проверка существующих рейтингов
SELECT user_id, rating, passenger_rating, full_name
FROM profiles
LIMIT 5;
```

**Ожидаемый результат:**
- ✅ Поле `passenger_rating` существует
- ✅ 3 функции созданы
- ✅ Триггер активен
- ✅ Все пользователи имеют `passenger_rating = 5.0`

---

## ⚠️ Если возникли ошибки

### Ошибка: "column already exists"
- Поле уже добавлено ранее
- Можно пропустить эту часть миграции или удалить поле и применить заново

### Ошибка: "function already exists"
- Функции уже созданы
- Миграция использует `CREATE OR REPLACE`, поэтому это нормально

### Ошибка: "permission denied"
- Убедитесь, что используете правильный проект
- Проверьте права доступа к БД

---

## 📊 Что произойдет после применения

1. ✅ Все пользователи получат `passenger_rating = 5.0`
2. ✅ При создании новых отзывов рейтинги будут обновляться автоматически
3. ✅ Водители увидят рейтинг пассажира в бронированиях
4. ✅ Пользователи увидят свой рейтинг пассажира в профиле

---

## 🎯 После применения

После успешного применения:
1. ✅ Проверьте работу через создание тестового отзыва
2. ✅ Убедитесь, что рейтинг отображается в UI
3. ✅ Протестируйте обновление рейтинга

**Готово!** 🎉
