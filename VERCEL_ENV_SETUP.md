# 🔧 Настройка переменных окружения в Vercel

## ⚠️ Ошибка: Missing VITE_SUPABASE_PUBLISHABLE_KEY

Если вы видите эту ошибку, значит переменные окружения не настроены в Vercel.

## 📋 Пошаговая инструкция

### 1. Откройте настройки проекта в Vercel

1. Войдите на [vercel.com](https://vercel.com)
2. Выберите ваш проект `ride-together`
3. Перейдите в **Settings** → **Environment Variables**

### 2. Добавьте переменные окружения

Нажмите **"Add New"** и добавьте следующие переменные:

#### Переменная 1: `VITE_SUPABASE_URL`

- **Name:** `VITE_SUPABASE_URL`
- **Value:** `https://xdlkilvotcnssarzugws.supabase.co`
- **Environment:** Выберите все (Production, Preview, Development)

#### Переменная 2: `VITE_SUPABASE_PUBLISHABLE_KEY`

- **Name:** `VITE_SUPABASE_PUBLISHABLE_KEY`
- **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhkbGtpbHZvdGNuc3Nhcnp1Z3dzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ3OTU1MzksImV4cCI6MjA4MDM3MTUzOX0.yrfb04FoSZEWmuq-pqJ57KZe27QG5bczqjMyO1yfAfg`
- **Environment:** Выберите все (Production, Preview, Development)

#### Переменная 3: `VITE_MAPBOX_TOKEN` (опционально)

- **Name:** `VITE_MAPBOX_TOKEN`
- **Value:** Ваш токен Mapbox (если используете карты)
- **Environment:** Выберите все (Production, Preview, Development)

### 3. Сохраните и передеплойте

1. Нажмите **"Save"** для каждой переменной
2. Перейдите в **Deployments**
3. Найдите последний деплой и нажмите **"Redeploy"** (три точки → Redeploy)
   - Или создайте новый деплой через **"Deploy"** → **"Redeploy"**

### 4. Проверьте результат

После передеплоя приложение должно работать без ошибок.

## 🔍 Как проверить, что переменные настроены

1. В Vercel Dashboard → Settings → Environment Variables
2. Вы должны увидеть все три переменные (или хотя бы первые две)
3. Убедитесь, что они включены для нужных окружений (Production, Preview, Development)

## ⚠️ Важные моменты

- ✅ Имена переменных должны быть **точно** такими: `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`
- ✅ Не используйте пробелы в именах
- ✅ Не используйте кавычки в значениях
- ✅ После добавления переменных **обязательно передеплойте** проект
- ✅ Переменные применяются только к новым деплоям

## 🚨 Если ошибка сохраняется

1. Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)
2. Убедитесь, что вы передеплоили проект после добавления переменных
3. Проверьте, что имена переменных написаны правильно (без опечаток)
4. Очистите кэш браузера и попробуйте снова

## 📞 Альтернативный способ (через CLI)

Если у вас установлен Vercel CLI:

```bash
cd /Users/ahmeddevops/Desktop/ride/ride-together
vercel env add VITE_SUPABASE_URL
# Введите значение: https://xdlkilvotcnssarzugws.supabase.co
# Выберите окружения: Production, Preview, Development

vercel env add VITE_SUPABASE_PUBLISHABLE_KEY
# Введите значение: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhkbGtpbHZvdGNuc3Nhcnp1Z3dzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ3OTU1MzksImV4cCI6MjA4MDM3MTUzOX0.yrfb04FoSZEWmuq-pqJ57KZe27QG5bczqjMyO1yfAfg
# Выберите окружения: Production, Preview, Development

vercel --prod  # Передеплой
```

---

**После настройки переменных окружения приложение должно работать корректно!** 🎉

