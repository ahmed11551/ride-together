# 🔧 Решение проблем при деплое

## ❌ Ошибка: "Missing VITE_SUPABASE_PUBLISHABLE_KEY environment variable"

**Проблема:**
При запуске приложения в браузере вы видите ошибку:
```
Uncaught Error: Missing VITE_SUPABASE_PUBLISHABLE_KEY environment variable. 
Please check your .env file and ensure VITE_SUPABASE_PUBLISHABLE_KEY is set.
```

**Причина:**
Переменные окружения не настроены в Vercel или настроены неправильно.

**Решение:**

1. **Откройте Vercel Dashboard:**
   - Войдите на [vercel.com](https://vercel.com)
   - Выберите ваш проект
   - Перейдите в **Settings** → **Environment Variables**

2. **Добавьте переменные окружения:**
   - Нажмите **"Add New"**
   - Добавьте следующие переменные:
     * **Name:** `VITE_SUPABASE_URL`
       **Value:** `https://xdlkilvotcnssarzugws.supabase.co`
       **Environment:** Выберите все (Production, Preview, Development)
     
     * **Name:** `VITE_SUPABASE_PUBLISHABLE_KEY`
       **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhkbGtpbHZvdGNuc3Nhcnp1Z3dzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ3OTU1MzksImV4cCI6MjA4MDM3MTUzOX0.yrfb04FoSZEWmuq-pqJ57KZe27QG5bczqjMyO1yfAfg`
       **Environment:** Выберите все (Production, Preview, Development)
     
     * **Name:** `VITE_GEOAPIFY_API_KEY` (опционально, для карт и маршрутов)
       **Value:** `6c75034c3c87419cb5b408a967c925e4`
       **Environment:** Выберите все (Production, Preview, Development)

3. **Передеплойте проект:**
   - После сохранения переменных перейдите в **Deployments**
   - Найдите последний деплой и нажмите **"Redeploy"**
   - Или создайте новый деплой

4. **Проверьте результат:**
   - После передеплоя приложение должно работать без ошибок

**Важно:**
- ✅ Имена переменных должны быть **точно** такими: `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`
- ✅ Не используйте пробелы в именах
- ✅ Не используйте кавычки в значениях
- ✅ После добавления переменных **обязательно передеплойте** проект

**Подробная инструкция:** См. файл `VERCEL_ENV_SETUP.md`

---

## ❌ Ошибка: "Имя содержит недопустимые символы"

### Причина:
Эта ошибка возникает при настройке переменных окружения в Vercel, если имя переменной содержит недопустимые символы.

### Решение:

**Правильные имена переменных:**
- ✅ `VITE_SUPABASE_URL`
- ✅ `VITE_SUPABASE_PUBLISHABLE_KEY`
- ✅ `VITE_MAPBOX_TOKEN`
- ✅ `API_KEY`
- ✅ `MY_VAR_123`

**Неправильные имена:**
- ❌ `VITE-SUPABASE-URL` (дефисы не допускаются)
- ❌ `123_VAR` (не может начинаться с цифры)
- ❌ `VAR NAME` (пробелы не допускаются)
- ❌ `VAR.NAME` (точки не допускаются)

### Как правильно добавить переменные в Vercel:

1. **В Vercel Dashboard:**
   - Settings → Environment Variables
   - Name: `VITE_SUPABASE_URL` (только буквы, цифры, подчеркивания)
   - Value: `https://xdlkilvotcnssarzugws.supabase.co`
   - Нажмите "Save"

2. **Через Vercel CLI:**
   ```bash
   vercel env add VITE_SUPABASE_URL
   # Введите значение при запросе
   ```

### Проверка:

Убедитесь, что:
- ✅ Имя переменной написано БЕЗ дефисов
- ✅ Используются только буквы, цифры и `_`
- ✅ Имя не начинается с цифры
- ✅ Нет пробелов в имени

---

## ❌ Ошибка: Build fails

### Возможные причины:

1. **Отсутствуют переменные окружения:**
   - Решение: Добавьте все необходимые переменные в Vercel Dashboard

2. **Неправильный формат переменных:**
   - Решение: Проверьте, что значения правильные (без лишних пробелов)

3. **Ошибки в коде:**
   - Решение: Проверьте логи сборки в Vercel Dashboard

---

## ❌ Ошибка: 404 на роутах

### Причина:
SPA routing не настроен правильно.

### Решение:
Убедитесь, что в `vercel.json` есть:
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

---

## ❌ Ошибка: Cannot connect to Supabase

### Причина:
Неправильные credentials или CORS настройки.

### Решение:

1. **Проверьте переменные окружения:**
   - Убедитесь, что `VITE_SUPABASE_URL` правильный
   - Убедитесь, что `VITE_SUPABASE_PUBLISHABLE_KEY` правильный (anon key, не service_role)

2. **Проверьте CORS в Supabase:**
   - Dashboard → Settings → API
   - Добавьте ваш Vercel домен в список разрешенных

3. **Проверьте RLS политики:**
   - Убедитесь, что политики настроены правильно

---

## ❌ Ошибка: Port already in use (локально)

### Решение:
```bash
# Измените порт в vite.config.ts
# Или остановите процесс на порту 8080
lsof -ti:8080 | xargs kill
```

---

## 📞 Нужна помощь?

- Проверьте логи в Vercel Dashboard
- Проверьте консоль браузера (F12)
- См. документацию: [Vercel Docs](https://vercel.com/docs)

