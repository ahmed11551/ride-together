# 🚀 Применить миграцию СЕЙЧАС

## ⚡ Самый быстрый способ (2 минуты)

### Шаг 1: Откройте Supabase Dashboard
👉 **https://app.supabase.com/project/vcjnvkdqjrqymnmqdvfr/sql/new**

(Или: Dashboard → Ваш проект → SQL Editor → New query)

### Шаг 2: Скопируйте и выполните

**Откройте файл:** `supabase/migrations/20250131000001_add_passenger_rating.sql`

**Скопируйте ВСЁ содержимое файла** (Ctrl+A, Ctrl+C)

**Вставьте в SQL Editor** и нажмите **Run** (или `Cmd+Enter`)

---

## ✅ Готово!

После выполнения вы увидите сообщение об успехе.

**Проверка:**
```sql
SELECT user_id, rating, passenger_rating, full_name
FROM profiles
LIMIT 5;
```

Все пользователи должны иметь `passenger_rating = 5.0`

---

## 📝 Что делает миграция

1. ✅ Добавляет поле `passenger_rating` в таблицу `profiles`
2. ✅ Устанавливает рейтинг 5.0 для всех существующих пользователей
3. ✅ Создает функции для автоматического обновления рейтингов
4. ✅ Создает триггер для обновления при создании отзывов
5. ✅ Обновляет существующие рейтинги на основе текущих отзывов

---

**Время выполнения:** ~5-10 секунд

**Безопасность:** ✅ Миграция использует `IF NOT EXISTS`, безопасна для повторного запуска
