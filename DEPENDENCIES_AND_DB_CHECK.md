# 📋 Проверка зависимостей и базы данных

## ✅ Зависимости

### Текущие зависимости в package.json:

**Production:**
- @sentry/node - мониторинг ошибок
- bcrypt - хэширование паролей
- cors - CORS middleware
- dotenv - переменные окружения
- express - веб-сервер
- express-rate-limit - rate limiting
- helmet - security headers
- jsonwebtoken - JWT токены
- nodemailer - отправка email
- pg - PostgreSQL клиент
- socket.io - WebSocket

**Development:**
- @types/* - типы для TypeScript
- tsx - TypeScript executor
- typescript - компилятор TypeScript

### ✅ Удалено:
- ❌ eslint (не использовался активно)
- ❌ zod (не использовался)

### ✅ Оставлено только необходимое

---

## 🗄️ База данных

### Текущая конфигурация в ecosystem.config.cjs:

Проверьте в файле `ecosystem.config.cjs` что там указано:

```javascript
DATABASE_URL: 'postgresql://gen_user:fn)un5%40K2oLrBJ@9d497bc2bf9dd679bd9834af.twc1.net:5432/default_db?sslmode=verify-full'
```

Это старая база данных от Timeweb.

### Нужно проверить:

1. **Какая база используется сейчас?**
   - Локальная на сервере REG.RU?
   - Или старая Timeweb?

2. **Если нужна новая локальная база:**

```bash
# На сервере проверьте
psql -U postgres -l

# Или
sudo -u postgres psql -l
```

---

## 🔧 Рекомендации:

1. **Если используете локальную базу:**
   - Обновите `DATABASE_URL` в `ecosystem.config.cjs`
   - Формат: `postgresql://user:password@localhost:5432/database_name`

2. **Если используете старую Timeweb:**
   - Это нормально, если база доступна
   - Но лучше перейти на локальную для производительности

---

## Проверка на сервере:

```bash
# Проверить переменные окружения PM2
pm2 show ride-backend | grep DATABASE_URL

# Проверить подключение к базе
psql $DATABASE_URL -c "SELECT version();"
```

