# 🔌 Как подключиться к серверу

## 📋 Информация о сервере:

- **IP адрес:** `194.67.124.123`
- **Пользователь:** `root`
- **Хостнейм:** `cv5600437.regru.cloud`
- **Порт SSH:** `22` (стандартный)

---

## 🚀 Варианты подключения:

### Вариант 1: Через скрипт (быстро)

```bash
cd /Users/ahmeddevops/Desktop/ride/ride-together
chmod +x connect.sh
./connect.sh
```

### Вариант 2: Через SSH напрямую

```bash
ssh root@194.67.124.123
```

### Вариант 3: С указанием ключа (если используется)

```bash
ssh -i ~/.ssh/your_key root@194.67.124.123
```

---

## 🔐 Если требуется пароль:

При подключении через SSH система запросит пароль. Введите пароль от сервера REG.RU.

**Где найти пароль:**
1. Зайдите в панель управления REG.RU
2. Откройте раздел "VPS" или "Виртуальные серверы"
3. Найдите ваш сервер `cv5600437`
4. Пароль обычно в разделе "Пароли" или "Управление доступом"

---

## 🔑 Если используете SSH ключ:

Если у вас настроен SSH ключ, подключение должно проходить без пароля.

**Проверить SSH ключи:**
```bash
ls -la ~/.ssh/
```

**Добавить SSH ключ на сервер** (если нужно):
```bash
ssh-copy-id root@194.67.124.123
```

---

## ✅ Проверка подключения:

После подключения вы должны увидеть приглашение:
```
root@cv5600437:~#
```

Проверить текущую директорию проекта:
```bash
cd /var/www/ride-together/server
pwd
ls -la
```

---

## 📝 Полезные команды на сервере:

```bash
# Перейти в директорию проекта
cd /var/www/ride-together/server

# Проверить статус PM2
pm2 status

# Посмотреть логи
pm2 logs ride-backend

# Проверить статус PostgreSQL
sudo systemctl status postgresql

# Проверить статус Nginx
sudo systemctl status nginx

# Выйти с сервера
exit
```

---

## 🆘 Если не можете подключиться:

1. **Проверьте интернет-соединение**
2. **Убедитесь, что IP адрес правильный:** `194.67.124.123`
3. **Проверьте, что SSH сервис запущен на сервере** (через веб-консоль REG.RU)
4. **Проверьте firewall** - порт 22 должен быть открыт
5. **Используйте веб-консоль REG.RU** для первого подключения

**Веб-консоль REG.RU:**
- Зайдите в панель управления REG.RU
- Откройте ваш VPS сервер
- Найдите кнопку "VNC" или "Консоль"
- Это позволит зайти на сервер без SSH

