# Настройки TTL для DNS

## 📊 Текущие настройки:

### ✅ Minimum (время кэширования негативных ответов):
- **Текущее значение:** `3h` (3 часа) ✅ **ПРАВИЛЬНО**
- Это оптимальное значение, оставьте как есть

### ⚠️ TTL (время кэширования положительных ответов):
- **Текущее значение:** `1d` (1 день = 86400 секунд) ⚠️ **МОЖНО УЛУЧШИТЬ**
- Это означает что DNS изменения будут кэшироваться до 24 часов
- Для быстрого распространения DNS лучше временно уменьшить

## 🚀 Рекомендации:

### Вариант 1: Быстрое распространение (рекомендуется сейчас)

1. **Измените TTL на:** `1h` (1 час = 3600 секунд)
   - Это минимальное значение для быстрого распространения
   - После получения SSL сертификата можно вернуть обратно к `1d`

2. **Minimum оставьте:** `3h` (3 часа) ✅

3. **Нажмите "Готово"**

**Почему это поможет:**
- DNS серверы будут кэшировать записи только 1 час вместо 24 часов
- Изменения DNS распространятся быстрее (вместо 24 часов займет 1-2 часа)
- После получения SSL можно вернуть обратно к `1d`

### Вариант 2: Оставить как есть

Если уже подождали и DNS записи распространились - можно оставить `1d`.

## ✅ Итоговые рекомендации:

**Сейчас (для быстрого распространения):**
- TTL: `1h` (1 час) ⬇️ уменьшите
- Minimum: `3h` (3 часа) ✅ оставьте

**После получения SSL (для оптимизации):**
- TTL: `1d` (1 день) ⬆️ верните обратно
- Minimum: `3h` (3 часа) ✅ оставьте

## 📝 Инструкция:

1. В модальном окне измените значение TTL:
   - Текущее: `1d`
   - Новое: `1h` (или просто `3600`)
   
2. Minimum оставьте: `3h`

3. Нажмите "Готово"

4. Подождите 30-60 минут и проверьте DNS:
   ```bash
   dig ridetogether.ru @8.8.8.8 +short
   dig api.ridetogether.ru @8.8.8.8 +short
   ```

5. После обновления DNS получите SSL сертификат

6. После получения SSL верните TTL обратно к `1d` (для оптимизации)

