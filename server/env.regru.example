# Production Environment Variables для REG.RU VPS
# Скопируйте этот файл в .env.production и заполните значения

# Database Connection
# Если PostgreSQL на том же VPS (рекомендуется):
DATABASE_URL=postgresql://ride_user:your_password@localhost:5432/ride_together
# Если PostgreSQL в приватной сети REG.RU:
# DATABASE_URL=postgresql://user:password@192.168.0.XXX:5432/database_name
# Если Managed PostgreSQL на REG.RU:
# DATABASE_URL=postgresql://user:password@db-host.reg.ru:5432/database_name

# JWT Configuration
# ВАЖНО: Сгенерируйте свой секретный ключ: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_YOUR_SECRET_KEY_MIN_32_CHARS_USE_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=production
HOST=0.0.0.0

# CORS Configuration
# Замените на ваш реальный домен
ALLOWED_ORIGINS=https://your-domain.ru,https://www.your-domain.ru
FRONTEND_URL=https://your-domain.ru

# WebSocket
WS_PORT=3001

# SSL/TLS (если используется напрямую через Node.js, обычно через Nginx)
SSL_KEY=/etc/letsencrypt/live/your-domain.ru/privkey.pem
SSL_CERT=/etc/letsencrypt/live/your-domain.ru/fullchain.pem

