# 🔍 Проверка строки 10 в dist/index.js

## Проблема:
Ошибка указывает на строку 10, позицию 17:
```
at file:///var/www/ride-together/server/dist/index.js:10:17
```

## Диагностика на сервере:

Выполните эти команды, чтобы увидеть, что на строке 10:

```bash
cd /var/www/ride-together/server/dist

# Показать строки 1-15
head -15 index.js | cat -n

# Показать все использования __dirname
grep -n "__dirname" index.js

# Показать конкретно строку 10
sed -n '10p' index.js
```

## Возможные проблемы:

1. **Прямое использование `__dirname`** (не в path.join)
2. **Использование в другой функции** (например, `path.resolve(__dirname)`)
3. **Использование в определении переменной**

## Решение:

После того как увидите строку 10, нужно будет либо:
- Заменить на `process.cwd()`
- Или добавить определение `__dirname` перед использованием

---

## Полное исправление (если не знаете что на строке 10):

```bash
cd /var/www/ride-together/server/dist

# Показать проблемную область
echo "Строки 1-20:"
head -20 index.js | cat -n

# Заменить все использования __dirname (кроме определений)
# Сначала path.join и path.resolve
sed -i 's/path\.join(__dirname,/path.join(process.cwd(),/g' index.js
sed -i 's/path\.resolve(__dirname,/path.resolve(process.cwd(),/g' index.js

# Проверить что получилось
node --check index.js
```

---

## Если проблема в определении __dirname:

Возможно, TypeScript скомпилировал определение `__dirname` неправильно. В этом случае нужно добавить правильное определение в начало файла или использовать только `process.cwd()`.

