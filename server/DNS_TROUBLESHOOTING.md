# Устранение проблем с DNS для ridetogether.ru

## 🔍 Текущая проблема:

DNS записи еще не обновились. Видно `NXDOMAIN` - это означает что:
1. DNS записи не настроены в REG.RU, ИЛИ
2. Используется неправильный IP адрес, ИЛИ
3. DNS еще не распространились (нужно подождать)

## ✅ Правильная настройка DNS в REG.RU:

### 1. Проверьте текущие записи в REG.RU:

Зайдите в панель управления REG.RU → Управление доменом `ridetogether.ru` → DNS-серверы и зона

### 2. Должны быть настроены DNS-серверы REG.RU:

```
ns1.reg.ru
ns2.reg.ru
```

### 3. Должны быть следующие A-записи:

| Имя | Тип | Значение | TTL |
|-----|-----|----------|-----|
| @ | A | **194.67.124.123** | 3600 |
| www | A | **194.67.124.123** | 3600 |
| api | A | **194.67.124.123** | 3600 |

**⚠️ ВАЖНО:** IP адрес должен быть `194.67.124.123` (публичный IP вашего VPS)

### 4. Если видите IP `95.163.244.138`:

Это **неправильный IP**! Нужно заменить на `194.67.124.123`

## 🔍 Проверка настроек:

### Проверка 1: DNS серверы домена

```bash
# На сервере или локально
dig NS ridetogether.ru @8.8.8.8
```

Должны вернуться:
```
ridetogether.ru.    IN    NS    ns1.reg.ru.
ridetogether.ru.    IN    NS    ns2.reg.ru.
```

### Проверка 2: A-записи

```bash
# Основной домен
dig A ridetogether.ru @ns1.reg.ru

# Поддомен www
dig A www.ridetogether.ru @ns1.reg.ru

# Поддомен api
dig A api.ridetogether.ru @ns1.reg.ru
```

Должны вернуться:
```
ridetogether.ru.    3600    IN    A    194.67.124.123
www.ridetogether.ru.    3600    IN    A    194.67.124.123
api.ridetogether.ru.    3600    IN    A    194.67.124.123
```

### Проверка 3: Глобальная проверка (после распространения)

```bash
# Через Google DNS (может занять время)
dig ridetogether.ru @8.8.8.8
dig www.ridetogether.ru @8.8.8.8
dig api.ridetogether.ru @8.8.8.8
```

## ⏰ Время распространения DNS:

- **Минимум:** 5-15 минут
- **Обычно:** 30 минут - 2 часа
- **Максимум:** 24 часа (редко)

## 🛠️ Что делать сейчас:

### Шаг 1: Проверьте настройки в REG.RU

1. Войдите в панель REG.RU
2. Перейдите в "Мои домены" → `ridetogether.ru`
3. Нажмите "DNS-серверы и зона"
4. Проверьте что DNS-серверы: `ns1.reg.ru` и `ns2.reg.ru`
5. Проверьте A-записи:
   - `@` → `194.67.124.123`
   - `www` → `194.67.124.123`
   - `api` → `194.67.124.123`

### Шаг 2: Если записи неправильные:

1. Удалите старые записи с неправильным IP
2. Добавьте новые записи с IP `194.67.124.123`
3. Сохраните изменения

### Шаг 3: Ожидание распространения

После сохранения изменений подождите 15-30 минут и проверьте:

```bash
# На сервере
dig ridetogether.ru @8.8.8.8 +short
dig api.ridetogether.ru @8.8.8.8 +short

# Должно вернуться:
# 194.67.124.123
```

### Шаг 4: После обновления DNS - получить SSL

```bash
certbot --nginx -d api.ridetogether.ru -d ridetogether.ru -d www.ridetogether.ru
```

## 🔧 Альтернатива: Проверка с других DNS серверов

Иногда разные DNS серверы обновляются с разной скоростью:

```bash
# Cloudflare DNS
dig ridetogether.ru @1.1.1.1 +short

# Yandex DNS
dig ridetogether.ru @77.88.8.8 +short

# REG.RU DNS (должно работать сразу)
dig ridetogether.ru @ns1.reg.ru +short
```

## ❓ Если не помогло:

1. **Проверьте что домен делегирован на REG.RU DNS:**
   ```bash
   whois ridetogether.ru | grep -i "Name Server"
   ```

2. **Свяжитесь с поддержкой REG.RU:**
   - Убедитесь что домен правильно делегирован
   - Проверьте что DNS зона активна

3. **Проверьте публичный IP сервера:**
   ```bash
   curl ifconfig.me
   # Должно вернуться: 194.67.124.123
   ```

## ✅ Чеклист:

- [ ] DNS-серверы: `ns1.reg.ru`, `ns2.reg.ru`
- [ ] A-запись `@` → `194.67.124.123`
- [ ] A-запись `www` → `194.67.124.123`
- [ ] A-запись `api` → `194.67.124.123`
- [ ] Изменения сохранены в REG.RU
- [ ] Прошло минимум 15 минут
- [ ] `dig ridetogether.ru @8.8.8.8 +short` возвращает `194.67.124.123`

