# 🔒 Настройка безопасности

## Шаг 1: Установка зависимостей

На сервере выполните:

```bash
cd /var/www/ride-together/server
npm install express-rate-limit helmet
npm install --save-dev @types/express-rate-limit
```

## Шаг 2: Применение изменений

После установки зависимостей нужно:
1. Перекомпилировать TypeScript: `npm run build`
2. Перезапустить PM2: `pm2 restart ride-backend --update-env`

## Что добавлено:

### Rate Limiting
- **Общий лимит**: 100 запросов за 15 минут на IP
- **Аутентификация**: 5 попыток входа за 15 минут
- **Создание контента**: 20 созданий за час
- **Сообщения**: 30 сообщений в минуту

### Security Headers (helmet)
- Content Security Policy (CSP)
- HSTS (принудительный HTTPS)
- Защита от clickjacking
- XSS Protection
- Отключение информации о сервере

## Настройка для production

После установки на сервере, middleware будет автоматически применяться ко всем запросам.

