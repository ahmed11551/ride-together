# 🔌 Подключение к серверу

## Информация о сервере

- **IP адрес:** `194.67.124.123`
- **Хост:** `cv5600437` (REG.RU)
- **Пользователь:** `root`
- **Порт SSH:** `22` (по умолчанию)

---

## Способ 1: SSH через терминал (macOS/Linux)

### Базовое подключение:

```bash
ssh root@194.67.124.123
```

или

```bash
ssh root@cv5600437
```

### При первом подключении:

При первом подключении система спросит подтверждение:
```
The authenticity of host '194.67.124.123' can't be established.
Are you sure you want to continue connecting (yes/no)?
```

Введите `yes` и нажмите Enter.

### Ввод пароля:

После подключения система запросит пароль. Введите пароль от сервера (пароль не отображается при вводе).

---

## Способ 2: SSH с указанием порта

Если SSH работает на другом порту:

```bash
ssh -p 22 root@194.67.124.123
```

---

## Способ 3: Использование SSH ключа (рекомендуется)

Если у вас настроен SSH ключ:

```bash
ssh -i ~/.ssh/your_key root@194.67.124.123
```

---

## Способ 4: Через REG.RU панель управления

1. Зайдите на https://www.reg.ru
2. Войдите в личный кабинет
3. Перейдите в раздел "VPS/Выделенные серверы"
4. Выберите ваш сервер
5. Нажмите "Консоль" или "SSH доступ"
6. Откроется веб-консоль в браузере

---

## Способ 5: Через SSH клиент (Windows)

### Использование PuTTY:

1. Скачайте PuTTY: https://www.putty.org/
2. Запустите PuTTY
3. В поле "Host Name" введите: `194.67.124.123`
4. Порт: `22`
5. Тип соединения: `SSH`
6. Нажмите "Open"
7. Введите логин: `root`
8. Введите пароль

### Использование Windows Terminal / PowerShell:

```powershell
ssh root@194.67.124.123
```

---

## После подключения

После успешного подключения вы увидите приглашение:

```
Welcome to Ubuntu 24.04.3 LTS
...
root@cv5600437:~#
```

Теперь вы можете выполнять команды на сервере.

---

## Полезные команды после подключения

```bash
# Перейти в директорию проекта
cd /var/www/ride-together/server

# Проверить статус PM2
pm2 status

# Посмотреть логи
pm2 logs ride-backend

# Проверить место на диске
df -h

# Проверить использование памяти
free -h
```

---

## Если забыли пароль

### Вариант 1: Сброс через REG.RU панель

1. Зайдите в панель управления REG.RU
2. Выберите ваш VPS
3. Найдите опцию "Сброс пароля" или "Управление доступом"
4. Установите новый пароль

### Вариант 2: Через REG.RU консоль

1. Используйте веб-консоль в панели REG.RU
2. Войдите как root
3. Измените пароль командой:
   ```bash
   passwd
   ```

---

## Проблемы с подключением

### Ошибка: "Connection refused"

**Причина:** SSH сервис не запущен или порт заблокирован

**Решение:**
- Проверьте статус сервера в панели REG.RU
- Убедитесь, что сервер включен

### Ошибка: "Permission denied"

**Причина:** Неверный пароль или пользователь

**Решение:**
- Проверьте правильность пароля
- Убедитесь, что используете пользователя `root`
- Попробуйте сбросить пароль через панель REG.RU

### Ошибка: "Host key verification failed"

**Причина:** Изменился SSH ключ сервера

**Решение:**
```bash
# Удалите старый ключ
ssh-keygen -R 194.67.124.123

# Подключитесь снова
ssh root@194.67.124.123
```

---

## Безопасность

⚠️ **Важно:**
- Не делитесь паролем от сервера
- Используйте SSH ключи вместо паролей (более безопасно)
- Регулярно обновляйте пароль
- Не отключайте firewall без необходимости

---

## Быстрая команда для подключения

Создайте алиас в `~/.bashrc` или `~/.zshrc`:

```bash
alias ride-server='ssh root@194.67.124.123'
```

Затем просто:
```bash
ride-server
```

---

## Готово! 🚀

После подключения к серверу вы можете выполнить установку улучшений:

```bash
cd /var/www/ride-together/server
./INSTALL_IMPROVEMENTS.sh
```

