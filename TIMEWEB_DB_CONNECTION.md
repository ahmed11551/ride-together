# 🔗 Подключение к Timeweb Cloud Database

## ✅ Данные подключения

```
Host: 9d497bc2bf9dd679bd9834af.twc1.net
Port: 5432
Database: default_db
Username: gen_user
Password: [ваш пароль]
SSL: Требуется (ca.crt)
```

## 📋 Быстрое подключение

### 1. Через psql (командная строка)

```bash
# Установите переменную окружения с паролем
export TIMEWEB_DB_PASSWORD='your-password'

# Используйте готовый скрипт
./scripts/connect-timeweb.sh
```

### 2. Через строку подключения

```bash
export TIMEWEB_DB_PASSWORD='your-password'
export PGSSLROOTCERT=./ca.crt

psql "postgresql://gen_user:$TIMEWEB_DB_PASSWORD@9d497bc2bf9dd679bd9834af.twc1.net:5432/default_db?sslmode=verify-full"
```

### 3. Через веб-интерфейс

Откройте: https://dbs.timeweb.com/?pgsql=77.95.206.10&username=gen_user&db=default_db&ns=public

## 🔐 SSL Сертификат

Сертификат `ca.crt` находится в корне проекта. Он автоматически используется скриптами.

## 📦 Импорт данных

```bash
# 1. Экспортируйте данные из Supabase
export SUPABASE_DB_PASSWORD='supabase-password'
./scripts/export-supabase-data.sh

# 2. Импортируйте в Timeweb
export TIMEWEB_DB_PASSWORD='timeweb-password'
./scripts/import-to-timeweb.sh supabase_backup_YYYYMMDD_HHMMSS.sql
```

## ⚙️ Настройка переменных окружения

Создайте файл `.env.timeweb` на основе `.env.timeweb.example`:

```bash
cp .env.timeweb.example .env.timeweb
# Отредактируйте .env.timeweb и укажите пароль
```

## 🚀 Backend сервер

Для backend сервера используйте переменные из `.env.timeweb`:

```bash
cd server
npm install
# Создайте .env на основе .env.timeweb
npm run dev
```

## ✅ Проверка подключения

```bash
# Простое подключение
./scripts/connect-timeweb.sh

# Проверка таблиц
psql -h 9d497bc2bf9dd679bd9834af.twc1.net -U gen_user -d default_db -c "\dt"
```

## 🔧 Устранение проблем

### Ошибка SSL

Убедитесь, что:
1. Сертификат `ca.crt` находится в корне проекта
2. Используется `sslmode=verify-full`
3. Указан правильный путь к сертификату

### Ошибка подключения

Проверьте:
1. Правильность хоста и порта
2. Доступность БД из вашей сети (может потребоваться VPN)
3. Правильность пароля

### Ошибка прав доступа

Убедитесь, что пользователь `gen_user` имеет необходимые права на БД `default_db`.
