# ✅ Реализация рейтинга пассажира

## 📋 Что было сделано

### 1. Миграция базы данных
- ✅ Добавлено поле `passenger_rating` в таблицу `profiles`
- ✅ Создана функция `update_passenger_rating()` для обновления рейтинга пассажира
- ✅ Создана функция `update_driver_rating()` для обновления рейтинга водителя
- ✅ Создана универсальная функция `update_user_ratings()` для обновления обоих рейтингов
- ✅ Создан триггер `trigger_update_ratings_on_review` для автоматического обновления при создании отзыва
- ✅ Обновлены существующие рейтинги на основе текущих отзывов

**Файл:** `supabase/migrations/20250131000001_add_passenger_rating.sql`

### 2. Обновление логики отзывов
- ✅ Обновлена функция `updateUserRating()` в `useReviews.ts`
- ✅ Теперь использует функцию БД `update_user_ratings` для обновления обоих рейтингов
- ✅ Добавлен fallback на старый метод при ошибке RPC

**Файл:** `src/hooks/useReviews.ts`

### 3. Обновление типов TypeScript
- ✅ Добавлено поле `passenger_rating` в интерфейс `Profile`
- ✅ Обновлены типы Supabase для таблицы `profiles`
- ✅ Обновлен интерфейс `RideBooking` для включения `passenger_rating`

**Файлы:**
- `src/hooks/useProfile.ts`
- `src/integrations/supabase/types.ts`
- `src/hooks/useRideBookings.ts`

### 4. Обновление UI
- ✅ Профиль пользователя: добавлено отображение рейтинга пассажира
- ✅ Карточки бронирований: показывают рейтинг пассажира для водителей
- ✅ Водители видят рейтинг пассажира при подтверждении бронирований

**Файлы:**
- `src/pages/Profile.tsx`
- `src/components/rides/RideBookings.tsx`

---

## 🚀 Как применить миграцию

### Способ 1: Через Supabase Dashboard (Рекомендуется)

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** → **New query**
4. Скопируйте содержимое файла `supabase/migrations/20250131000001_add_passenger_rating.sql`
5. Вставьте и нажмите **Run**

### Способ 2: Через Supabase CLI

```bash
cd /Users/ahmeddevops/Desktop/ride/ride-together
supabase db push
```

---

## ✅ Проверка после применения

### 1. Проверка поля в БД
```sql
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'profiles'
AND column_name = 'passenger_rating';
```

### 2. Проверка функций
```sql
SELECT proname, prosrc
FROM pg_proc
WHERE proname IN ('update_passenger_rating', 'update_driver_rating', 'update_user_ratings');
```

### 3. Проверка триггера
```sql
SELECT tgname, tgrelid::regclass
FROM pg_trigger
WHERE tgname = 'trigger_update_ratings_on_review';
```

### 4. Тестовая проверка
```sql
-- Создайте тестовый отзыв и проверьте обновление рейтинга
-- Рейтинг должен обновиться автоматически через триггер
```

---

## 📊 Как это работает

### Логика обновления рейтинга

1. **При создании отзыва:**
   - Триггер `trigger_update_ratings_on_review` автоматически вызывает `update_user_ratings()`
   - Функция определяет, является ли пользователь водителем или пассажиром
   - Обновляет соответствующий рейтинг

2. **Рейтинг водителя (`rating`):**
   - Рассчитывается на основе отзывов от пассажиров
   - Обновляется при каждом новом отзыве от пассажира

3. **Рейтинг пассажира (`passenger_rating`):**
   - Рассчитывается на основе отзывов от водителей
   - Обновляется при каждом новом отзыве от водителя

### Отображение в UI

- **Профиль:** Показывает оба рейтинга (водитель и пассажир)
- **Бронирования:** Водители видят рейтинг пассажира при подтверждении
- **По умолчанию:** Новые пользователи получают рейтинг 5.0

---

## 🎯 Результат

После применения миграции:

1. ✅ Все пользователи имеют рейтинг пассажира (по умолчанию 5.0)
2. ✅ Рейтинг автоматически обновляется при создании отзывов
3. ✅ Водители видят рейтинг пассажира при подтверждении бронирований
4. ✅ Пользователи видят свой рейтинг пассажира в профиле

---

## 📝 Следующие шаги

После успешного применения миграции можно:

1. Протестировать создание отзывов и проверить обновление рейтингов
2. Добавить фильтры по рейтингу пассажира в поиске (опционально)
3. Добавить уведомления при изменении рейтинга (опционально)

---

## ⚠️ Важные замечания

1. **Резервное копирование:** Рекомендуется сделать бэкап БД перед применением
2. **Время выполнения:** Миграция может занять несколько секунд при большом количестве пользователей
3. **Обратная совместимость:** Старый код продолжит работать, так как `passenger_rating` имеет значение по умолчанию

---

**Статус:** ✅ Готово к применению
