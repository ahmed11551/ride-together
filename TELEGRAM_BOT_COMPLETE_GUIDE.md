# 📱 Полная инструкция по настройке Telegram бота RideConnect

## 📋 Содержание

1. [Создание бота через BotFather](#1-создание-бота-через-botfather)
2. [Настройка Mini App](#2-настройка-mini-app)
3. [Настройка Supabase Edge Function](#3-настройка-supabase-edge-function)
4. [Применение миграций базы данных](#4-применение-миграций-базы-данных)
5. [Настройка Webhook](#5-настройка-webhook)
6. [Настройка переменных окружения](#6-настройка-переменных-окружения)
7. [Тестирование бота](#7-тестирование-бота)
8. [Решение проблем](#8-решение-проблем)

---

## 1. Создание бота через BotFather

### Шаг 1.1: Откройте BotFather

1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Нажмите "Запустить" или отправьте `/start`

### Шаг 1.2: Создайте нового бота

1. Отправьте команду `/newbot`
2. BotFather спросит: **"Alright, a new bot. How are we going to call it? Please choose a name for your bot."**
3. Введите имя бота (например: `RideConnect` или `Ride Together`)
   - Это имя будет отображаться в списке контактов
   - Может содержать пробелы и эмодзи

4. BotFather спросит: **"Good. Now let's choose a username for your bot. It must end in `bot`. Like this, for example: TetrisBot or tetris_bot."**
5. Введите username (например: `RideConnectBot` или `ride_connect_bot`)
   - Должен заканчиваться на `bot`
   - Может содержать только латинские буквы, цифры и подчеркивания
   - Должен быть уникальным

6. BotFather ответит:
   ```
   Done! Congratulations on your new bot. You will find it at t.me/YourBotUsername. 
   You can now add a description, about section and profile picture for your bot using /setdescription, /setabouttext and /setuserpic.

   Use this token to access the HTTP API:
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
   ```

### Шаг 1.3: Сохраните токен бота

**⚠️ ВАЖНО:** Сохраните токен бота в безопасном месте! Он понадобится для настройки webhook.

Токен выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890`

### Шаг 1.4: Настройте описание бота (опционально)

1. Отправьте `/setdescription`
2. Выберите вашего бота
3. Введите описание (например: "Платформа для совместных поездок. Найди попутчиков или стань водителем!")

### Шаг 1.5: Настройте about текст (опционально)

1. Отправьте `/setabouttext`
2. Выберите вашего бота
3. Введите короткое описание (например: "Найди попутчиков для поездки")

### Шаг 1.6: Загрузите фото профиля (опционально)

1. Отправьте `/setuserpic`
2. Выберите вашего бота
3. Отправьте изображение (квадратное, рекомендуется 512x512px)

---

## 2. Настройка Mini App

### Шаг 2.1: Создайте Mini App

1. В BotFather отправьте команду `/newapp`
2. Выберите вашего бота из списка
3. BotFather спросит: **"Please choose a title for your app."**
   - Введите: `RideConnect` или `Ride Together`

4. BotFather спросит: **"Please provide a short description of your app."**
   - Введите: `Платформа для совместных поездок`

5. BotFather спросит: **"Please send me a photo for your app."**
   - Отправьте квадратное изображение (рекомендуется 512x512px)
   - Это будет иконка Mini App

6. BotFather спросит: **"Please provide a short name for your app (up to 32 characters)."**
   - Введите короткое имя (например: `rideconnect`)
   - Будет использоваться в URL: `https://t.me/YourBot/rideconnect`

7. BotFather спросит: **"Please provide a Web App URL."**
   - Введите URL вашего приложения (например: `https://your-app.vercel.app`)
   - Это должен быть полный URL с `https://`

8. BotFather спросит: **"Please provide a GIF animation (optional)."**
   - Можете отправить GIF или пропустить, отправив `/skip`

9. BotFather спросит: **"Please provide a short name for your app (up to 32 characters)."**
   - Это уже было, просто подтвердите

10. BotFather предоставит ссылку на Mini App:
    ```
    https://t.me/YourBot/rideconnect
    ```

### Шаг 2.2: Проверьте Mini App

1. Откройте ссылку, которую дал BotFather
2. Проверьте, что приложение открывается корректно
3. Если нужно изменить URL, используйте `/myapps` → выберите бота → `Edit` → `Web App URL`

---

## 3. Настройка Supabase Edge Function

### Шаг 3.1: Установите Supabase CLI

```bash
# macOS
brew install supabase/tap/supabase

# Windows (через Scoop)
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase

# Linux
npm install -g supabase
```

### Шаг 3.2: Войдите в Supabase

```bash
supabase login
```

Следуйте инструкциям для входа через браузер.

### Шаг 3.3: Свяжите проект

```bash
cd /path/to/ride-together
supabase link --project-ref your-project-ref
```

`your-project-ref` можно найти в URL вашего Supabase проекта:
- URL: `https://app.supabase.com/project/abcdefghijklmnop`
- Project ref: `abcdefghijklmnop`

### Шаг 3.4: Деплой Edge Function

```bash
supabase functions deploy telegram-bot
```

Если функция еще не существует, создайте её:

```bash
supabase functions new telegram-bot
```

Затем скопируйте содержимое из `supabase/functions/telegram-bot/index.ts` в созданный файл.

### Шаг 3.5: Проверьте деплой

После деплоя вы увидите URL функции:
```
https://abcdefghijklmnop.supabase.co/functions/v1/telegram-bot
```

Сохраните этот URL - он понадобится для webhook.

---

## 4. Применение миграций базы данных

### Шаг 4.1: Примените миграции локально (для тестирования)

```bash
supabase migration up
```

### Шаг 4.2: Примените миграции на продакшн

```bash
supabase db push
```

Или через Supabase Dashboard:
1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в `Database` → `Migrations`
4. Нажмите `New migration`
5. Скопируйте содержимое файлов:
   - `supabase/migrations/20250129000000_add_subscriptions.sql`
   - `supabase/migrations/20250129000001_add_support_tickets.sql`
6. Нажмите `Run migration`

### Шаг 4.3: Проверьте созданные таблицы

В Supabase Dashboard → `Database` → `Tables` должны появиться:
- `subscriptions`
- `bot_users`
- `referrals`
- `support_tickets`
- `bot_reviews`

---

## 5. Настройка Webhook

### Шаг 5.1: Получите URL вашей Edge Function

URL выглядит так:
```
https://abcdefghijklmnop.supabase.co/functions/v1/telegram-bot
```

Замените `abcdefghijklmnop` на ваш project ref.

### Шаг 5.2: Установите Webhook через API

**Вариант 1: Через curl (рекомендуется)**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://abcdefghijklmnop.supabase.co/functions/v1/telegram-bot"
  }'
```

Замените:
- `<YOUR_BOT_TOKEN>` на токен от BotFather
- `abcdefghijklmnop` на ваш Supabase project ref

**Вариант 2: Через браузер**

Откройте в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://abcdefghijklmnop.supabase.co/functions/v1/telegram-bot
```

### Шаг 5.3: Проверьте Webhook

**Вариант 1: Через API**

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

**Вариант 2: Через браузер**

Откройте:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

Должен вернуться JSON с информацией о webhook:
```json
{
  "ok": true,
  "result": {
    "url": "https://abcdefghijklmnop.supabase.co/functions/v1/telegram-bot",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Шаг 5.4: Удаление Webhook (если нужно)

Если нужно удалить webhook:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

---

## 6. Настройка переменных окружения

### Шаг 6.1: Переменные в Supabase Edge Function

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в `Project Settings` → `Edge Functions` → `Secrets`
4. Добавьте следующие секреты:

| Ключ | Значение | Описание |
|------|----------|----------|
| `TELEGRAM_BOT_TOKEN` | `123456789:ABC...` | Токен бота от BotFather |
| `APP_URL` | `https://your-app.vercel.app` | URL вашего приложения |
| `SUPABASE_URL` | `https://abcdefghijklmnop.supabase.co` | URL вашего Supabase проекта (уже должен быть) |
| `SUPABASE_SERVICE_ROLE_KEY` | `eyJhbGc...` | Service Role ключ (уже должен быть) |

**Как найти Service Role Key:**
1. В Supabase Dashboard → `Project Settings` → `API`
2. Найдите `service_role` ключ в разделе `Project API keys`
3. ⚠️ **ВАЖНО:** Никогда не публикуйте этот ключ! Он имеет полный доступ к БД.

### Шаг 6.2: Переменные в Vercel (для фронтенда)

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в `Settings` → `Environment Variables`
4. Добавьте (опционально, только если нужно в UI):

| Ключ | Значение | Описание |
|------|----------|----------|
| `VITE_TELEGRAM_BOT_TOKEN` | `123456789:ABC...` | Токен бота (только для UI компонентов) |

**⚠️ Примечание:** Токен бота в Vercel нужен только если вы используете компоненты, которые обращаются к API бота напрямую. Обычно это не требуется, так как все обрабатывается через Edge Function.

### Шаг 6.3: Локальные переменные (для разработки)

Создайте файл `.env.local` в корне проекта:

```env
# Supabase
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGc...

# Telegram (опционально)
VITE_TELEGRAM_BOT_TOKEN=123456789:ABC...

# Yandex Maps
VITE_YANDEX_MAPS_API_KEY=your_yandex_key

# Другие переменные...
```

---

## 7. Тестирование бота

### Шаг 7.1: Базовое тестирование

1. Найдите вашего бота в Telegram (по username)
2. Нажмите "Запустить" или отправьте `/start`
3. Проверьте, что бот отвечает приветственным сообщением
4. Проверьте, что кнопки работают

### Шаг 7.2: Тестирование команд

Отправьте следующие команды и проверьте ответы:

- `/start` - должно показать главное меню
- `/help` - должно показать справку
- `/support` - должно открыть раздел поддержки
- `/review` - должно открыть раздел отзывов
- `/ticket Описание проблемы` - должно создать тикет

### Шаг 7.3: Тестирование кнопок

1. Нажмите "🚀 Открыть приложение"
   - Должно открыться ваше приложение

2. Нажмите "💬 Поддержка"
   - Должен открыться раздел поддержки
   - Нажмите "📝 Создать тикет"
   - Проверьте создание тикета

3. Нажмите "⭐ Отзывы"
   - Должен открыться раздел отзывов
   - Нажмите "⭐ Оставить отзыв"
   - Выберите оценку
   - Проверьте сохранение отзыва

### Шаг 7.4: Проверка базы данных

1. Откройте Supabase Dashboard → `Database` → `Table Editor`
2. Проверьте таблицы:
   - `bot_users` - должен появиться ваш пользователь
   - `support_tickets` - если создали тикет
   - `bot_reviews` - если оставили отзыв

### Шаг 7.5: Проверка логов

1. Откройте Supabase Dashboard → `Edge Functions` → `telegram-bot` → `Logs`
2. Проверьте, что нет ошибок
3. При отправке сообщения боту должны появляться логи

---

## 8. Решение проблем

### Проблема 1: Бот не отвечает

**Решение:**
1. Проверьте webhook:
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
   ```
2. Проверьте логи Edge Function в Supabase Dashboard
3. Убедитесь, что все переменные окружения установлены
4. Проверьте, что Edge Function задеплоена

### Проблема 2: Webhook не устанавливается

**Решение:**
1. Убедитесь, что URL начинается с `https://`
2. Проверьте, что URL доступен (откройте в браузере)
3. Убедитесь, что токен бота правильный
4. Попробуйте удалить и установить webhook заново

### Проблема 3: Ошибки в Edge Function

**Решение:**
1. Проверьте логи в Supabase Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте синтаксис кода в `index.ts`
4. Убедитесь, что миграции применены

### Проблема 4: Mini App не открывается

**Решение:**
1. Проверьте URL в BotFather (`/myapps` → выберите бота → `Edit`)
2. Убедитесь, что приложение доступно по этому URL
3. Проверьте, что приложение работает в обычном браузере
4. Убедитесь, что URL начинается с `https://`

### Проблема 5: Тикеты/отзывы не сохраняются

**Решение:**
1. Проверьте, что миграции применены
2. Проверьте RLS политики в Supabase Dashboard
3. Убедитесь, что `SUPABASE_SERVICE_ROLE_KEY` установлен
4. Проверьте логи Edge Function

### Проблема 6: Бот отвечает, но кнопки не работают

**Решение:**
1. Проверьте формат `reply_markup` в коде
2. Убедитесь, что `callback_data` не превышает 64 байта
3. Проверьте логи Edge Function при нажатии на кнопку

---

## 📝 Чеклист настройки

- [ ] Бот создан через BotFather
- [ ] Токен бота сохранен
- [ ] Mini App создан и настроен
- [ ] Supabase CLI установлен
- [ ] Edge Function задеплоена
- [ ] Миграции применены
- [ ] Webhook установлен
- [ ] Все переменные окружения настроены
- [ ] Бот отвечает на `/start`
- [ ] Кнопки работают
- [ ] Mini App открывается
- [ ] Тикеты создаются
- [ ] Отзывы сохраняются
- [ ] Данные появляются в БД

---

## 🔗 Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Supabase Edge Functions Documentation](https://supabase.com/docs/guides/functions)
- [BotFather](https://t.me/BotFather)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи Edge Function
2. Проверьте логи Supabase
3. Проверьте webhook через API
4. Убедитесь, что все шаги выполнены

---

## 🎉 Готово!

После выполнения всех шагов ваш бот должен:
- ✅ Отвечать на команды
- ✅ Показывать меню с кнопками
- ✅ Открывать Mini App
- ✅ Создавать тикеты поддержки
- ✅ Сохранять отзывы
- ✅ Регистрировать пользователей в БД

Удачи! 🚀

