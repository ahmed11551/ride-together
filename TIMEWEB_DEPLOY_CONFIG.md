# ⚙️ Правильные настройки деплоя на Timeweb Cloud

## ❌ Текущие настройки (НЕПРАВИЛЬНО)

- **Фреймворк:** Angular ❌
- **Версия окружения:** 24
- **Команда сборки:** (пусто) ❌
- **Зависимости:** (пусто)
- **Директория сборки:** (пусто) ❌

## ✅ Правильные настройки для React/Vite проекта

### 1. Фреймворк
**Выберите:** `React` или `Vite` или `Static Site` (если есть такая опция)
- ❌ НЕ выбирайте Angular
- ✅ Выберите React/Vite/Static Site

### 2. Версия окружения
**Выберите:** `20` или `22` (Node.js версия)
- Node.js 20 LTS рекомендуется
- Или Node.js 22 (последняя LTS)

### 3. Команда сборки
**Введите:** `npm run build`
```
npm run build
```

### 4. Зависимости
**Оставьте пустым** или введите:
```
npm ci
```
(Обычно Timeweb автоматически запускает `npm install` или `npm ci`)

### 5. Директория сборки
**Введите:** `dist`
```
dist
```

## 📋 Полная конфигурация

```
Фреймворк: React (или Vite, или Static Site)
Версия окружения: 20 (Node.js)
Команда сборки: npm run build
Зависимости: (пусто или npm ci)
Директория сборки: dist
```

## 🔍 Проверка

После настройки убедитесь, что:

1. ✅ Фреймворк НЕ Angular
2. ✅ Команда сборки: `npm run build`
3. ✅ Директория сборки: `dist`
4. ✅ Node.js версия: 20 или 22

## ⚠️ Важно

Если в Timeweb нет опции "React" или "Vite", выберите:
- **Static Site** (для статического фронтенда)
- Или **Node.js** (если нужно запускать dev server)

Для нашего проекта (React SPA) лучше всего подходит **Static Site**, так как мы собираем статические файлы в `dist/` и отдаем их через Nginx.

## 🚀 После настройки

1. Сохраните настройки
2. Добавьте Environment Variables (см. `DEPLOY_FIX_TIMEWEB.md`)
3. Запустите деплой
4. Проверьте работу сайта
