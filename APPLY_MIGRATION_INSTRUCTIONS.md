# 🚀 Инструкция по применению миграции для улучшения стабильности БД

## ⚠️ Важно: Эта миграция критична для стабильности базы данных!

Миграция добавляет:
- ✅ Защиту от race conditions в бронированиях
- ✅ CHECK constraints для целостности данных
- ✅ Дополнительные индексы для производительности
- ✅ Улучшенный триггер для обновления мест

## 📋 Способ 1: Через Supabase Dashboard (Рекомендуется)

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на [https://app.supabase.com](https://app.supabase.com)
2. Войдите в свой аккаунт
3. Выберите проект (ID: `vcjnvkdqjrqymnmqdvfr`)

### Шаг 2: Откройте SQL Editor
1. В левом меню нажмите на **"SQL Editor"**
2. Нажмите **"New query"**

### Шаг 3: Скопируйте и выполните миграцию
1. Откройте файл: `supabase/migrations/20250131000000_improve_database_stability.sql`
2. Скопируйте **весь** содержимое файла
3. Вставьте в SQL Editor в Supabase Dashboard
4. Нажмите **"Run"** или `Ctrl+Enter` (Windows) / `Cmd+Enter` (Mac)

### Шаг 4: Проверьте результат
После выполнения вы должны увидеть:
- ✅ Сообщение об успешном выполнении
- ✅ Список созданных constraints и индексов

**Если возникли ошибки:**
- Проверьте, что все предыдущие миграции применены
- Убедитесь, что нет конфликтующих constraints
- Проверьте логи ошибок в Supabase Dashboard

## 📋 Способ 2: Через Supabase CLI (Если установлен)

### Установка Supabase CLI (если не установлен)
```bash
# macOS
brew install supabase/tap/supabase

# или через npm
npm install -g supabase
```

### Применение миграции
```bash
cd /Users/ahmeddevops/Desktop/ride/ride-together

# Логин в Supabase
supabase login

# Свяжите проект (если еще не связано)
supabase link --project-ref vcjnvkdqjrqymnmqdvfr

# Примените миграцию
supabase db push
```

## ✅ Проверка успешного применения

После применения миграции выполните следующие проверки:

### 1. Проверка constraints
```sql
-- Проверьте, что constraints созданы
SELECT 
    conname AS constraint_name,
    contype AS constraint_type,
    pg_get_constraintdef(oid) AS definition
FROM pg_constraint
WHERE conrelid = 'public.rides'::regclass
AND conname LIKE 'check_%';
```

### 2. Проверка функции триггера
```sql
-- Проверьте, что функция обновлена
SELECT 
    proname AS function_name,
    pg_get_functiondef(oid) AS definition
FROM pg_proc
WHERE proname = 'update_seats_on_booking';
```

### 3. Проверка индексов
```sql
-- Проверьте новые индексы
SELECT 
    indexname,
    indexdef
FROM pg_indexes
WHERE tablename IN ('rides', 'bookings', 'messages', 'profiles')
AND indexname LIKE 'idx_%'
ORDER BY tablename, indexname;
```

### 4. Тестовая проверка работы
```sql
-- Создайте тестовую поездку
INSERT INTO public.rides (
    driver_id, from_city, from_address, to_city, to_address,
    departure_date, departure_time, price, seats_total, seats_available
) VALUES (
    auth.uid(), 'Москва', 'Тест', 'СПб', 'Тест',
    CURRENT_DATE + 1, '10:00', 1000, 4, 4
) RETURNING id;

-- Попробуйте забронировать больше мест, чем доступно (должна быть ошибка)
-- Это проверит работу CHECK constraint
```

## 🔍 Что изменилось в миграции

### 1. CHECK Constraints
- `check_seats_available` - проверяет, что свободных мест >= 0 и <= общего количества
- `check_seats_total` - проверяет, что общее количество мест от 1 до 10
- `check_price` - проверяет, что цена от 0 до 1,000,000
- `check_departure_date` - проверяет, что дата не в прошлом
- `check_seats_booked` - проверяет, что забронировано от 1 до 10 мест
- `check_rating` - проверяет, что рейтинг от 0 до 5

### 2. Улучшенный триггер `update_seats_on_booking`
- Использует `SELECT FOR UPDATE` для защиты от race conditions
- Проверяет статус поездки перед бронированием
- Проверяет доступность мест перед обновлением
- Правильно обрабатывает отмены бронирований

### 3. Новые индексы
- `idx_rides_status_date_active` - для поиска активных поездок
- `idx_bookings_status_ride` - для поиска активных бронирований
- `idx_messages_created_at` - для сортировки сообщений
- `idx_profiles_rating` - для поиска по рейтингу
- `idx_rides_driver_status` - для поиска поездок водителя

### 4. Новая функция `is_ride_participant`
- Оптимизированная функция для проверки участия в поездке
- Можно использовать в RLS политиках для messages

## ⚠️ Важные замечания

1. **Резервное копирование**: Перед применением миграции рекомендуется сделать бэкап базы данных через Supabase Dashboard

2. **Время выполнения**: Миграция может занять несколько секунд, особенно если в базе много данных

3. **Ошибки при применении**: Если миграция не применяется:
   - Проверьте, что все предыдущие миграции применены
   - Убедитесь, что нет конфликтующих constraints
   - Проверьте логи в Supabase Dashboard

4. **Откат**: Если нужно откатить миграцию, выполните:
   ```sql
   -- Удаление constraints
   ALTER TABLE public.rides DROP CONSTRAINT IF EXISTS check_seats_available;
   ALTER TABLE public.rides DROP CONSTRAINT IF EXISTS check_seats_total;
   ALTER TABLE public.rides DROP CONSTRAINT IF EXISTS check_price;
   ALTER TABLE public.rides DROP CONSTRAINT IF EXISTS check_departure_date;
   ALTER TABLE public.bookings DROP CONSTRAINT IF EXISTS check_seats_booked;
   ALTER TABLE public.bookings DROP CONSTRAINT IF EXISTS check_total_price;
   ALTER TABLE public.profiles DROP CONSTRAINT IF EXISTS check_rating;
   ALTER TABLE public.profiles DROP CONSTRAINT IF EXISTS check_trips_count;
   
   -- Удаление индексов (опционально)
   DROP INDEX IF EXISTS idx_rides_status_date_active;
   DROP INDEX IF EXISTS idx_bookings_status_ride;
   DROP INDEX IF EXISTS idx_messages_created_at;
   DROP INDEX IF EXISTS idx_profiles_rating;
   DROP INDEX IF EXISTS idx_rides_driver_status;
   
   -- Восстановление старой функции (если нужно)
   -- Скопируйте старую версию из предыдущей миграции
   ```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Supabase Dashboard → Logs
2. Проверьте, что все зависимости установлены
3. Убедитесь, что у вас есть права на изменение схемы БД

## ✅ После применения

После успешного применения миграции:
1. ✅ База данных защищена от race conditions
2. ✅ Данные валидируются на уровне БД
3. ✅ Производительность улучшена за счет новых индексов
4. ✅ Стабильность системы повышена

**Рекомендуется протестировать бронирования после применения миграции!**
