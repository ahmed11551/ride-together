# Руководство по миграции с Lovable на чистый Supabase

Это руководство поможет вам полностью избавиться от зависимости от Lovable Cloud и перейти на прямой Supabase.

## ✅ Что уже сделано

- ✅ Удален `lovable-tagger` из зависимостей
- ✅ Удалены все упоминания Lovable из кода
- ✅ Проект настроен для работы с чистым Supabase
- ✅ Обновлена документация

## 📋 Шаги миграции

### 1. Создайте свой Supabase проект

1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Выберите регион (рекомендуется ближайший к вашим пользователям)
4. Запишите Project URL и API keys

### 2. Примените миграции базы данных

В вашем проекте есть миграции в папке `supabase/migrations/`:

1. Откройте Supabase Dashboard → SQL Editor
2. Выполните миграции в порядке:
   - `20251213021331_7a00ec87-3392-4efa-8263-d4d1fe272d41.sql` (основная схема)
   - `20251213021356_a5c9a937-87f4-46c5-8704-a93d5b858511.sql` (исправления)
   - `20250127000000_add_reports_and_admin.sql` (жалобы и админка)

Или используйте Supabase CLI:

```bash
# Установите Supabase CLI
npm install -g supabase

# Инициализируйте проект
supabase init

# Примените миграции
supabase db push
```

### 3. Настройте переменные окружения

Обновите `.env` файл с новыми значениями из вашего Supabase проекта:

```env
# Supabase (из вашего нового проекта)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here

# Mapbox (опционально)
VITE_MAPBOX_TOKEN=your-mapbox-token-here
```

**Где найти:**
- Supabase Dashboard → Settings → API
- Project URL → `VITE_SUPABASE_URL`
- anon/public key → `VITE_SUPABASE_PUBLISHABLE_KEY`

### 4. Миграция данных (если есть)

Если у вас уже есть данные в Lovable Cloud:

#### Вариант A: Экспорт через Supabase Dashboard
1. В старом проекте (Lovable Cloud) откройте Database → Tables
2. Для каждой таблицы: Actions → Export data → CSV/JSON
3. В новом проекте: Import data

#### Вариант B: SQL дамп
1. Используйте `pg_dump` или Supabase CLI для экспорта
2. Импортируйте в новый проект

#### Вариант C: Ручная миграция через SQL
Если данных немного, можно скопировать вручную через SQL Editor.

### 5. Настройте аутентификацию

Supabase Auth работает так же, как в Lovable:
- Email/Password уже настроен
- Проверьте настройки в Authentication → Settings
- При необходимости настройте email templates

### 6. Настройте Realtime

Realtime для чата уже настроен в миграциях:
```sql
ALTER PUBLICATION supabase_realtime ADD TABLE public.messages;
```

Проверьте в Supabase Dashboard → Database → Replication, что таблица `messages` включена.

### 7. Деплой фронтенда

#### Vercel (рекомендуется):

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Задеплойте:
```bash
vercel
```

3. Добавьте переменные окружения в Vercel Dashboard:
   - Settings → Environment Variables
   - Добавьте все переменные из `.env`

#### Netlify:

1. Установите Netlify CLI:
```bash
npm install -g netlify-cli
```

2. Задеплойте:
```bash
netlify deploy --prod
```

3. Добавьте переменные окружения в Netlify Dashboard

### 8. Настройте домен (опционально)

В Vercel/Netlify:
1. Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи

## 🔍 Проверка после миграции

### Тестирование функционала:

1. ✅ Регистрация и вход
2. ✅ Создание поездки
3. ✅ Поиск поездок
4. ✅ Бронирование
5. ✅ Чат (realtime)
6. ✅ Отзывы и рейтинги
7. ✅ Админ панель

### Проверка производительности:

- Откройте DevTools → Network
- Проверьте, что запросы идут на ваш Supabase URL
- Проверьте время ответа

## 💰 Сравнение стоимости

### Lovable Cloud:
- Платный план от $29/месяц
- Ограничения на трафик
- Нет отдельных dev/prod окружений

### Supabase + Vercel:
- **Бесплатно** до:
  - 500MB база данных
  - 2GB bandwidth
  - 50,000 запросов/месяц (Supabase)
  - 100GB bandwidth (Vercel)
- Отдельные проекты для dev/prod
- Полный контроль

## 🎯 Преимущества после миграции

1. **Экономия**: Бесплатный tier покрывает большинство MVP
2. **Контроль**: Полный доступ к БД, настройкам, логам
3. **Масштабируемость**: Легко добавить Edge Functions, Storage
4. **Независимость**: Не зависите от Lovable
5. **Гибкость**: Можете использовать любые инструменты

## 🐛 Решение проблем

### Проблема: "Invalid API key"
- Проверьте, что используете правильный `anon` key (не `service_role`)
- Убедитесь, что ключ скопирован полностью

### Проблема: Realtime не работает
- Проверьте, что таблица `messages` включена в Replication
- Проверьте RLS политики

### Проблема: Миграции не применяются
- Убедитесь, что выполняете миграции в правильном порядке
- Проверьте права доступа в Supabase

## 📚 Дополнительные ресурсы

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase CLI](https://supabase.com/docs/guides/cli)
- [Vercel Documentation](https://vercel.com/docs)
- [Netlify Documentation](https://docs.netlify.com)

## ✅ Чеклист миграции

- [ ] Создан новый Supabase проект
- [ ] Применены все миграции
- [ ] Обновлены переменные окружения
- [ ] Мигрированы данные (если есть)
- [ ] Протестирована аутентификация
- [ ] Протестирован realtime чат
- [ ] Задеплоен фронтенд на Vercel/Netlify
- [ ] Добавлены переменные окружения в хостинг
- [ ] Протестированы все функции
- [ ] Настроен домен (опционально)

---

**Поздравляем!** Теперь ваш проект полностью независим от Lovable и работает напрямую с Supabase. 🎉

