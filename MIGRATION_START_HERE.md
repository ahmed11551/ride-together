# 🚀 Начало миграции - Шаг 1

## ✅ Что делаем сейчас

### Шаг 1: Создание БД в Timeweb Cloud

1. **Откройте ваш проект:**
   👉 https://timeweb.cloud/my/projects/2005839

2. **Создайте PostgreSQL базу данных:**
   - Перейдите в раздел **"Databases"**
   - Нажмите **"Create Database"**
   - Выберите **PostgreSQL**
   - Выберите конфигурацию:
     - **Минимум:** 1 ГБ RAM, 1 ГБ Storage (~300₽/мес)
     - **Рекомендуется:** 2 ГБ RAM, 5 ГБ Storage (~500₽/мес)
   - Нажмите **"Create"**

3. **Запишите данные подключения:**
   После создания БД вы увидите:
   ```
   Host: xxx.timeweb.cloud
   Port: 5432
   Database: your_database_name
   Username: your_username
   Password: your_password (сохраните его!)
   ```

4. **Сохраните данные в файл:**
   Создайте файл `.env.timeweb` (НЕ коммитьте в Git!):
   ```env
   TIMEWEB_DB_HOST=xxx.timeweb.cloud
   TIMEWEB_DB_PORT=5432
   TIMEWEB_DB_NAME=your_database_name
   TIMEWEB_DB_USER=your_username
   TIMEWEB_DB_PASSWORD=your_password
   ```

---

## 📋 После создания БД

**Сообщите мне, когда БД создана, и я помогу с:**
1. Экспортом данных из Supabase
2. Импортом в Timeweb Cloud
3. Применением миграций

---

## 🔧 Параллельно: Подготовка к экспорту

Пока создаётся БД, можно подготовить экспорт данных:

### Вариант A: Через Supabase Dashboard (Проще)

1. Откройте: https://app.supabase.com/project/vcjnvkdqjrqymnmqdvfr
2. Database → Backups
3. Нажмите "Create backup"
4. Дождитесь завершения
5. Скачайте SQL файл

### Вариант B: Через pg_dump (Точнее)

Нужны данные подключения из Supabase:
1. Settings → Database → Connection string
2. Скопируйте Host, Port, Database, User
3. Получите Password (нажмите "Reveal")

Затем используйте скрипт:
```bash
./scripts/export-supabase-data.sh
```

---

## ⏳ Время выполнения

- **Создание БД:** ~5 минут
- **Экспорт данных:** ~10-30 минут (зависит от размера)
- **Импорт данных:** ~10-30 минут

**Общее время для Фазы 1:** ~1-2 часа

---

## ✅ Готовы начать?

1. Создайте БД в Timeweb Cloud
2. Сохраните данные подключения
3. Сообщите мне - продолжу с экспортом данных!

**Или скажите, если нужна помощь с созданием БД!** 🚀
