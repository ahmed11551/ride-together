# 🚀 Быстрый запуск Ride Together

## ✅ Статус проекта

**Бэкенд полностью настроен!**
- ✅ Supabase проект создан
- ✅ Все таблицы созданы
- ✅ RLS политики настроены
- ✅ Realtime для чата работает
- ✅ .env файл с credentials готов

**Frontend готов к запуску:**
- ✅ Все файлы на месте
- ✅ package.json настроен
- ⚠️ Нужно установить зависимости (npm install)

## 📋 Шаги для запуска

### Шаг 1: Установите Node.js (если еще не установлен)

**Вариант A - через Homebrew (рекомендуется):**
```bash
brew install node
```

**Вариант B - через официальный сайт:**
1. Откройте [nodejs.org](https://nodejs.org/)
2. Скачайте LTS версию для macOS
3. Установите

**Вариант C - через nvm (для управления версиями):**
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.zshrc
nvm install --lts
nvm use --lts
```

### Шаг 2: Установите зависимости

```bash
cd /Users/ahmeddevops/Desktop/ride/ride-together
npm install
```

Это займет несколько минут при первом запуске.

### Шаг 3: Запустите приложение

```bash
npm run dev
```

Вы увидите:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:8080/
  ➜  Network: use --host to expose
```

### Шаг 4: Откройте в браузере

Откройте: **http://localhost:8080**

## 🎯 Первые шаги после запуска

1. **Зарегистрируйтесь:**
   - Нажмите "Войти" → "Регистрация"
   - Заполните форму
   - Профиль создастся автоматически

2. **Назначьте себя администратором:**
   
   В Supabase Dashboard → SQL Editor выполните:
   ```sql
   -- Найдите ваш user_id
   SELECT id, email FROM auth.users;
   
   -- Назначьте администратором (замените 'your-user-id')
   UPDATE public.profiles 
   SET is_admin = true 
   WHERE user_id = 'your-user-id';
   ```

3. **Создайте первую поездку:**
   - Нажмите "Опубликовать поездку"
   - Заполните форму
   - Выберите маршрут на карте (если настроен Mapbox)

## 🔧 Проверка работы

### Проверьте подключение к Supabase:

1. Откройте DevTools (F12)
2. Вкладка Console
3. Не должно быть ошибок подключения

### Проверьте таблицы:

В Supabase Dashboard → Table Editor должны быть:
- ✅ profiles
- ✅ rides
- ✅ bookings
- ✅ reviews
- ✅ messages
- ✅ reports
- ✅ driver_info

## 📝 Важные файлы

- `.env` - переменные окружения (уже настроены)
- `BACKEND_SETUP.md` - детали о бэкенде
- `README.md` - полная документация
- `START.md` - краткая инструкция

## 🐛 Решение проблем

### Проблема: "npm: command not found"
**Решение:** Установите Node.js (см. Шаг 1)

### Проблема: "Cannot find module"
**Решение:** 
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Port 8080 already in use"
**Решение:** Измените порт в `vite.config.ts` или остановите процесс на порту 8080

### Проблема: Ошибки подключения к Supabase
**Решение:** Проверьте `.env` файл, убедитесь что credentials правильные

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет работать локально и готово к разработке!

---

**Нужна помощь?** См. `BACKEND_SETUP.md` или `README.md`

