# 🔧 Исправление проблемы с деплоем

## Проблема

Страница не загружается на `https://ahmed11551-ride-together-d23f.twc1.net/`

## Возможные причины

1. **Неправильная сборка** - фронтенд не собран или собран неправильно
2. **Неправильные переменные окружения** - `VITE_API_URL` не установлен
3. **Проблемы с роутингом** - SPA не настроен правильно
4. **Проблемы с сервером** - бэкенд не запущен или недоступен

## Решение

### 1. Проверьте сборку локально

```bash
# Создайте .env
cp env.example .env

# Заполните VITE_API_URL
echo "VITE_API_URL=https://your-backend-url.twc1.net" >> .env

# Соберите проект
npm run build

# Проверьте локально
npm run preview
```

Если локально работает, значит проблема в деплое.

### 2. Проверьте деплой на Timeweb

#### Вариант A: Статический хостинг

1. Убедитесь, что загружены **все файлы** из папки `dist/`
2. Убедитесь, что `.htaccess` скопирован в `dist/` (для SPA роутинга)
3. Проверьте, что `index.html` находится в корне

#### Вариант B: App Platform

1. Проверьте логи сборки в Timeweb Dashboard
2. Убедитесь, что переменные окружения установлены:
   ```
   VITE_API_URL=https://your-backend-url.twc1.net
   ```
3. Проверьте, что Start Command правильный:
   ```
   npx serve -s dist -l 3000
   ```

### 3. Проверьте бэкенд

```bash
# Проверьте health endpoint
curl https://your-backend-url.twc1.net/health

# Должен вернуть: {"status":"ok","timestamp":"..."}
```

### 4. Проверьте консоль браузера

Откройте DevTools (F12) и проверьте:
- Ошибки в Console
- Запросы в Network tab
- Проверьте, что `index.html` загружается

### 5. Быстрое исправление

Если ничего не помогает, создайте простой `index.html` для теста:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Ride Together</title>
</head>
<body>
    <h1>Ride Together</h1>
    <p>Если вы видите это, значит сервер работает.</p>
    <script>
        console.log('Test page loaded');
    </script>
</body>
</html>
```

Если и это не работает, проблема в настройке хостинга.

## Чеклист

- [ ] Проект собирается локально (`npm run build`)
- [ ] `dist/` папка содержит файлы
- [ ] `.htaccess` скопирован в `dist/` (для статического хостинга)
- [ ] Переменные окружения установлены в Timeweb
- [ ] Бэкенд доступен и отвечает на `/health`
- [ ] `index.html` находится в корне `dist/`
- [ ] Нет ошибок в логах Timeweb

## Следующие шаги

После исправления:
1. Проверьте работу приложения
2. Проверьте консоль браузера на ошибки
3. Проверьте Network tab на успешные запросы к API
