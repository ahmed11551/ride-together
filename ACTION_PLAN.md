# 📋 План действий для запуска

## ✅ Что уже готово

- ✅ База данных: схема создана (`TIMEWEB_FULL_SCHEMA.sql`)
- ✅ Бэкенд: API endpoints готовы (auth, websocket)
- ✅ Фронтенд: React приложение готово
- ✅ Инструкции: документация по деплою создана
- ✅ Очистка: удалена устаревшая документация

## 🔴 Критично (сделать сейчас)

### 1. Настроить переменные окружения

**Бэкенд (`server/.env`):**
```env
TIMEWEB_DB_HOST=your-host.twc1.net
TIMEWEB_DB_PORT=5432
TIMEWEB_DB_NAME=default_db
TIMEWEB_DB_USER=gen_user
TIMEWEB_DB_PASSWORD=your-password
TIMEWEB_DB_SSL=true
TIMEWEB_DB_SSL_CERT=./ca.crt
JWT_SECRET=сгенерируйте: openssl rand -base64 32
ALLOWED_ORIGINS=http://localhost:8080
```

**Фронтенд (`.env`):**
```env
VITE_API_URL=https://your-backend-url.twc1.net
```

### 2. Задеплоить бэкенд

1. Timeweb App Platform → Создать приложение
2. GitHub репозиторий → Root: `server`
3. Build: `npm install && npm run build`
4. Start: `npm start`
5. Port: `3001`
6. Добавить переменные из `server/.env`

### 3. Задеплоить фронтенд

**Вариант A (рекомендуется):**
```bash
# Локально
npm install
echo "VITE_API_URL=https://your-backend-url" > .env
npm run build

# Загрузить dist/ на Timeweb Static Hosting
# Скопировать .htaccess в dist/
```

**Вариант B:**
- Timeweb App Platform → Новое приложение
- Root: `.`
- Build: `npm install && npm run build`
- Start: `npx serve -s dist -l 3000`
- Environment: `VITE_API_URL=https://your-backend-url`

### 4. Обновить CORS

После получения домена фронтенда:
- В бэкенд приложении обновить `ALLOWED_ORIGINS`
- Перезапустить бэкенд

## 🟡 Средний приоритет (после запуска)

### 1. Миграция хуков с Supabase на API

Нужно обновить хуки, которые используют Supabase:
- `useProfile.ts` - использовать `apiClient` вместо `supabase`
- `useRides.ts` - создать API endpoints для поездок
- `useBookings.ts` - создать API endpoints для бронирований
- `useMessages.ts` - использовать WebSocket вместо Supabase Realtime
- `useReviews.ts` - создать API endpoints для отзывов

### 2. Создать недостающие API endpoints

В `server/api/` нужно добавить:
- `rides/` - CRUD для поездок
- `bookings/` - управление бронированиями
- `reviews/` - отзывы и рейтинги
- `messages/` - сообщения (через WebSocket)

### 3. Тестирование

- Протестировать регистрацию/вход
- Протестировать создание поездки
- Протестировать бронирование
- Протестировать чат

## 🟢 Низкий приоритет (полировка)

### 1. Оптимизация

- Lazy loading для компонентов
- Оптимизация изображений
- Кэширование API запросов

### 2. Дополнительные фичи

- Push-уведомления (VAPID)
- Yandex Maps интеграция
- Telegram Bot

### 3. Документация

- API документация (Swagger)
- User guide
- Developer guide

## 🐛 Известные проблемы

1. **Страница не загружается** - см. `DEPLOY_FIX.md`
2. **CORS ошибки** - обновить `ALLOWED_ORIGINS`
3. **404 на роутах** - проверить `.htaccess` в `dist/`

## 📚 Полезные файлы

- `QUICK_START.md` - Быстрый старт
- `START_HERE_TIMEWEB.md` - Навигация
- `TIMEWEB_QUICK_DEPLOY.md` - Быстрый деплой
- `TIMEWEB_FULL_DEPLOY.md` - Подробная инструкция
- `DEPLOY_FIX.md` - Исправление проблем

## ⏱️ Оценка времени

- **Критично**: 2-3 часа (деплой бэкенда и фронтенда)
- **Средний приоритет**: 3-5 дней (миграция хуков, API endpoints)
- **Низкий приоритет**: 1-2 недели (оптимизация, полировка)

## 🎯 Цель

Полностью рабочий проект на Timeweb с:
- ✅ Работающей регистрацией/входом
- ✅ Созданием и поиском поездок
- ✅ Бронированием
- ✅ Чат (WebSocket)
- ✅ Отзывами и рейтингами
