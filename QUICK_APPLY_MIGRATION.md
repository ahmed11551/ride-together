# ⚡ Быстрое применение миграции рейтинга пассажира

## 📋 Инструкция (3 шага)

### 1️⃣ Откройте Supabase Dashboard
👉 https://app.supabase.com → Ваш проект (ID: `vcjnvkdqjrqymnmqdvfr`)

### 2️⃣ Откройте SQL Editor
👉 Левое меню → **SQL Editor** → **New query**

### 3️⃣ Скопируйте и выполните миграцию

**Файл для копирования:** `supabase/migrations/20250131000001_add_passenger_rating.sql`

1. Откройте файл в редакторе
2. Выделите всё (Ctrl+A / Cmd+A)
3. Скопируйте (Ctrl+C / Cmd+C)
4. Вставьте в SQL Editor в Supabase
5. Нажмите **Run** (или `Cmd+Enter` / `Ctrl+Enter`)

---

## ✅ Проверка после применения

Выполните этот SQL для проверки:

```sql
-- Проверка поля
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'profiles'
AND column_name = 'passenger_rating';

-- Проверка рейтингов пользователей
SELECT user_id, rating, passenger_rating, full_name
FROM profiles
LIMIT 5;
```

**Ожидаемый результат:**
- ✅ Поле `passenger_rating` существует
- ✅ Все пользователи имеют `passenger_rating = 5.0`

---

## 🎯 Готово!

После применения миграции:
- ✅ Все пользователи получат рейтинг пассажира 5.0
- ✅ Рейтинги будут обновляться автоматически при создании отзывов
- ✅ Водители увидят рейтинг пассажира в бронированиях
- ✅ Пользователи увидят свой рейтинг пассажира в профиле

---

**Если возникли проблемы:** См. `APPLY_PASSENGER_RATING_MIGRATION.md`
