# Пример конфигурации Nginx для Ride Together на REG.RU

# Backend API (на поддомене или /api)
server {
    listen 80;
    server_name api.your-domain.ru;  # или your-domain.ru если через /api
    
    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для WebSocket
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
}

# Frontend (главный домен)
server {
    listen 80;
    server_name your-domain.ru www.your-domain.ru;
    root /var/www/html;
    index index.html;
    
    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # SPA routing - все запросы на index.html
    location / {
        try_files $uri $uri/ /index.html;
        
        # Кэширование статики
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # API прокси (если API на том же домене)
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # WebSocket прокси
    location /socket.io {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # Таймауты для WebSocket
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
    
    # Безопасность
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# После настройки SSL (certbot автоматически обновит):
# server {
#     listen 443 ssl http2;
#     server_name your-domain.ru www.your-domain.ru;
#     
#     ssl_certificate /etc/letsencrypt/live/your-domain.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.ru/privkey.pem;
#     
#     # ... остальная конфигурация как выше
# }
# 
# server {
#     listen 80;
#     server_name your-domain.ru www.your-domain.ru;
#     return 301 https://$server_name$request_uri;
# }

