# 📋 Пошаговая инструкция: Деплой фронтенда на Vercel

## Предварительные требования

✅ Бэкенд задеплоен на Timeweb и доступен по URL  
✅ У вас есть аккаунт на [Vercel](https://vercel.com)  
✅ Репозиторий подключен к GitHub  

## Шаг 1: Подготовка репозитория

### 1.1 Убедитесь, что все изменения закоммичены

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push
```

### 1.2 Проверьте структуру проекта

Убедитесь, что в корне проекта есть:
- `package.json`
- `vite.config.ts`
- `vercel.json` (уже создан)
- `src/` папка с кодом

## Шаг 2: Создание проекта в Vercel

### 2.1 Вход в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Войдите через GitHub (если еще не вошли)

### 2.2 Создание нового проекта

1. Нажмите **"Add New Project"** или **"New Project"**
2. Выберите ваш GitHub репозиторий (`ride-together` или название вашего репо)
3. Если репозиторий не виден:
   - Нажмите **"Adjust GitHub App Permissions"**
   - Разрешите доступ к нужным репозиториям

### 2.3 Настройка проекта

**Framework Preset:**
- Выберите **"Vite"** (или оставьте "Other" - Vercel определит автоматически)

**Root Directory:**
- Если репозиторий содержит только фронтенд: оставьте пустым
- Если репозиторий содержит и фронт, и бэкенд: укажите `ride-together` (или название папки с фронтендом)

**Build and Output Settings:**
- **Build Command**: `npm run build` (или оставьте пустым - Vercel определит)
- **Output Directory**: `dist` (или оставьте пустым - Vercel определит)
- **Install Command**: `npm install` (или оставьте пустым)

**⚠️ НЕ НАЖИМАЙТЕ "Deploy" ЕЩЕ!** Сначала нужно настроить переменные окружения.

## Шаг 3: Настройка переменных окружения

### 3.1 Добавление переменных

1. В форме создания проекта найдите раздел **"Environment Variables"**
2. Или после создания проекта: **Settings** → **Environment Variables**

### 3.2 Обязательные переменные

Добавьте следующие переменные:

#### VITE_API_URL (ОБЯЗАТЕЛЬНО)
```
Name: VITE_API_URL
Value: https://your-backend-url.twc1.net
Environment: Production, Preview, Development
```

**Где взять URL:**
- Это URL вашего бэкенда на Timeweb
- Пример: `https://api-12345.twc1.net` или `https://your-app.twc1.net`

#### VITE_WS_URL (опционально)
```
Name: VITE_WS_URL
Value: wss://your-backend-url.twc1.net
Environment: Production, Preview, Development
```

**Примечание:** Если не указан, будет использован `VITE_API_URL` с заменой `http(s)` на `ws(s)`

### 3.3 Опциональные переменные

Добавьте по необходимости:

```
VITE_YANDEX_MAPS_API_KEY=your-yandex-key
VITE_TELEGRAM_BOT_TOKEN=your-telegram-bot-token
VITE_SENTRY_DSN=your-sentry-dsn
VITE_GA_MEASUREMENT_ID=your-ga-id
```

### 3.4 Сохранение переменных

Нажмите **"Save"** или **"Add"** для каждой переменной

## Шаг 4: Деплой

### 4.1 Запуск деплоя

1. После настройки всех переменных нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)

### 4.2 Мониторинг деплоя

В процессе деплоя вы увидите:
- Установку зависимостей
- Сборку проекта (`npm run build`)
- Деплой файлов

### 4.3 Результат

После успешного деплоя:
- Вы получите URL вида: `your-app-abc123.vercel.app`
- Или кастомный домен, если настроен

## Шаг 5: Проверка деплоя

### 5.1 Откройте сайт

1. Нажмите на URL в Vercel Dashboard
2. Или откройте `https://your-app-abc123.vercel.app` в браузере

### 5.2 Проверка консоли

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Console**
3. Убедитесь, что нет ошибок подключения к API

### 5.3 Тестирование функционала

1. Попробуйте зарегистрироваться
2. Попробуйте войти
3. Проверьте, что данные сохраняются

### 5.4 Проверка Network

1. В DevTools → **Network**
2. Проверьте запросы к API:
   - Должны идти на ваш `VITE_API_URL`
   - Должны возвращать успешные ответы (200, 201)

## Шаг 6: Обновление CORS на бэкенде

### 6.1 Получение домена Vercel

После деплоя запишите ваш домен:
- Production: `your-app.vercel.app` или кастомный домен
- Preview: `your-app-git-branch-username.vercel.app`

### 6.2 Обновление ALLOWED_ORIGINS

1. Откройте [Timeweb Cloud](https://timeweb.cloud/my/projects/2005839)
2. Перейдите в ваше бэкенд приложение
3. Откройте **"Переменные окружения"**
4. Найдите `ALLOWED_ORIGINS`
5. Обновите значение:
   ```
   https://your-app.vercel.app,https://www.your-app.vercel.app
   ```
   Или для preview веток:
   ```
   https://your-app.vercel.app,https://*.vercel.app
   ```

### 6.3 Перезапуск бэкенда

1. В Timeweb Cloud → Ваше приложение
2. Нажмите **"Перезапустить"** или **"Redeploy"**
3. Дождитесь перезапуска

### 6.4 Проверка CORS

1. Обновите страницу фронтенда
2. Попробуйте выполнить действие (регистрация, вход)
3. В DevTools → Network проверьте заголовки ответа:
   - Должен быть `Access-Control-Allow-Origin: https://your-app.vercel.app`

## Шаг 7: Настройка кастомного домена (опционально)

### 7.1 Добавление домена в Vercel

1. В Vercel Dashboard → Ваш проект → **Settings** → **Domains**
2. Нажмите **"Add Domain"**
3. Введите ваш домен (например: `app.yourdomain.com`)
4. Следуйте инструкциям по настройке DNS

### 7.2 Обновление переменных

После добавления кастомного домена:

1. **В Vercel**: Переменные окружения обновятся автоматически
2. **В Timeweb**: Обновите `ALLOWED_ORIGINS` на новый домен
3. Перезапустите бэкенд

## Troubleshooting

### Ошибка сборки

**Проблема**: Build failed

**Решение**:
1. Проверьте логи в Vercel Dashboard → Deployments → View Function Logs
2. Убедитесь, что все зависимости указаны в `package.json`
3. Проверьте, что `vite.config.ts` корректен

### Ошибка подключения к API

**Проблема**: `Failed to fetch` или CORS ошибка

**Решение**:
1. Проверьте `VITE_API_URL` в Vercel (Settings → Environment Variables)
2. Убедитесь, что URL правильный и доступен
3. Проверьте `ALLOWED_ORIGINS` на бэкенде
4. Убедитесь, что бэкенд перезапущен после изменения CORS

### Ошибка 404 на роутах

**Проблема**: При переходе на `/profile` или другие роуты получаете 404

**Решение**:
1. Проверьте `vercel.json` - должен содержать rewrites для SPA
2. Убедитесь, что `rewrites` настроены правильно (уже настроено)

### Переменные окружения не работают

**Проблема**: `VITE_API_URL` undefined

**Решение**:
1. Убедитесь, что переменные начинаются с `VITE_`
2. Перезапустите деплой после добавления переменных
3. Проверьте, что переменные добавлены для нужных окружений (Production, Preview, Development)

## Автоматические деплои

После первого деплоя Vercel будет автоматически деплоить:
- **Production**: при push в `main`/`master` ветку
- **Preview**: при создании pull request или push в другие ветки

## Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation](https://vercel.com/docs)
- [Vite + Vercel Guide](https://vercel.com/guides/deploying-vite-with-vercel)

## Следующий шаг

После успешного деплоя:
1. ✅ Обновите CORS на бэкенде
2. ✅ Протестируйте все функции
3. ✅ Настройте мониторинг (Sentry, Analytics)
