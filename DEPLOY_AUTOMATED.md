# 🤖 Автоматизированная подготовка к деплою

## Быстрый старт

### 1. Запустите скрипт подготовки

```bash
./scripts/prepare-deploy.sh
```

Этот скрипт:
- ✅ Проверит наличие всех необходимых файлов
- ✅ Проверит структуру проекта
- ✅ Установит зависимости (если нужно)
- ✅ Выполнит тестовую сборку
- ✅ Создаст `server/.env` из примера (если не существует)

### 2. Проверьте готовность

```bash
./scripts/check-deploy.sh
```

Этот скрипт проверит:
- ✅ Настройку переменных окружения
- ✅ Наличие необходимых файлов
- ✅ Готовность к сборке
- ✅ Git статус

### 3. Отредактируйте server/.env

Откройте `server/.env` и заполните:

```env
TIMEWEB_DB_HOST=ваш-хост.twc1.net
TIMEWEB_DB_NAME=default_db
TIMEWEB_DB_USER=gen_user
TIMEWEB_DB_PASSWORD=ваш-пароль
JWT_SECRET=сгенерируйте-командой: openssl rand -base64 32
```

### 4. Следуйте инструкциям

После подготовки следуйте:
- `КОПИРУЙ_В_TIMEWEB.txt` - для деплоя бэкенда
- `КОПИРУЙ_В_VERCEL.txt` - для деплоя фронтенда
- `ОБНОВИ_CORS.txt` - для обновления CORS

## Что делают скрипты

### prepare-deploy.sh

1. Проверяет наличие всех необходимых файлов
2. Проверяет структуру `server/`
3. Проверяет наличие `ca.crt`
4. Создает `server/.env` из примера (если не существует)
5. Устанавливает зависимости (если нужно)
6. Выполняет тестовую сборку бэкенда и фронтенда

### check-deploy.sh

1. Проверяет настройку переменных в `server/.env`
2. Проверяет длину `JWT_SECRET` (минимум 32 символа)
3. Проверяет наличие `ca.crt`
4. Проверяет наличие собранных файлов
5. Проверяет git статус

## Ручная подготовка

Если скрипты не работают, выполните вручную:

```bash
# 1. Установка зависимостей
cd server && npm install && cd ..
npm install

# 2. Создание .env
cp server/env.example server/.env
# Отредактируйте server/.env

# 3. Тестовая сборка
cd server && npm run build && cd ..
npm run build

# 4. Проверка
./scripts/check-deploy.sh
```

## Troubleshooting

### Скрипт не запускается

```bash
chmod +x scripts/prepare-deploy.sh
chmod +x scripts/check-deploy.sh
```

### Ошибка сборки

Проверьте:
1. Все зависимости установлены (`npm install`)
2. TypeScript установлен (`npm list typescript`)
3. Нет синтаксических ошибок в коде

### Переменные не найдены

Убедитесь, что в `server/.env`:
- Нет пробелов вокруг `=`
- Значения не в кавычках (если не требуется)
- Нет комментариев на той же строке

## Следующие шаги

После успешной подготовки:
1. ✅ Следуйте `КОПИРУЙ_В_TIMEWEB.txt`
2. ✅ Следуйте `КОПИРУЙ_В_VERCEL.txt`
3. ✅ Следуйте `ОБНОВИ_CORS.txt`
