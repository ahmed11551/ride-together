# Быстрый старт: Деплой на Vercel + Timeweb

## 🚀 Быстрая настройка

### 1. База данных (Timeweb)

```bash
# 1. Откройте Timeweb Cloud SQL Editor
# 2. Скопируйте содержимое TIMEWEB_FULL_SCHEMA.sql
# 3. Выполните скрипт
```

### 2. Бэкенд (Timeweb)

```bash
cd server

# Создайте .env
cp env.example .env

# Отредактируйте .env:
# - Укажите данные БД из Timeweb
# - Установите JWT_SECRET (минимум 32 символа)
# - Укажите ALLOWED_ORIGINS (пока оставьте пустым, обновите после деплоя Vercel)

# Скопируйте ca.crt в папку server

# Деплой через Timeweb App Platform или Docker
```

### 3. Фронтенд (Vercel)

```bash
# В Vercel Dashboard → Settings → Environment Variables

VITE_API_URL=https://your-backend-url.twc1.net
VITE_WS_URL=wss://your-backend-url.twc1.net  # опционально

# Другие переменные (опционально):
VITE_YANDEX_MAPS_API_KEY=...
VITE_TELEGRAM_BOT_TOKEN=...
VITE_SENTRY_DSN=...
```

### 4. Обновите CORS на бэкенде

После деплоя Vercel, обновите `ALLOWED_ORIGINS` в `.env` бэкенда:

```env
ALLOWED_ORIGINS=https://your-app.vercel.app,https://www.your-app.vercel.app
```

## 📋 Чеклист

- [ ] База данных создана и схема применена
- [ ] Бэкенд задеплоен и доступен по URL
- [ ] Переменные окружения настроены в Vercel
- [ ] Фронтенд задеплоен
- [ ] CORS обновлен на бэкенде
- [ ] Проверена регистрация/вход
- [ ] Проверено подключение WebSocket

## 🔧 Генерация JWT_SECRET

```bash
openssl rand -base64 32
```

## 📚 Подробная документация

См. `DEPLOY_VERCEL_TIMEWEB.md` для детальных инструкций.
