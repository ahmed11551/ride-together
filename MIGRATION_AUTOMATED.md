# 🤖 Автоматическая миграция - Инструкция

## ✅ Что готово

1. ✅ Сертификат `ca.crt` - найден в проекте
2. ✅ Скрипты миграции - созданы и готовы
3. ✅ Конфигурация Timeweb - настроена

## 📋 Что нужно сделать

### Вариант 1: Через Supabase Dashboard (Самый простой)

1. **Экспорт данных:**
   - Откройте: https://app.supabase.com/project/vcjnvkdqjrqymnmqdvfr
   - Database → Backups → Create backup
   - Скачайте SQL файл

2. **Импорт в Timeweb:**
   - Откройте: https://dbs.timeweb.com/?pgsql=77.95.206.10&username=gen_user&db=default_db&ns=public
   - SQL Editor → Вставьте содержимое backup файла
   - Выполните

### Вариант 2: Через командную строку (Требует PostgreSQL клиент)

1. **Установите PostgreSQL клиент:**
   ```bash
   # macOS
   brew install postgresql
   
   # Ubuntu/Debian
   sudo apt-get install postgresql-client
   ```

2. **Запустите автоматическую миграцию:**
   ```bash
   cd /Users/ahmeddevops/Desktop/ride/ride-together
   ./scripts/migrate-to-timeweb.sh
   ```

   Скрипт запросит пароли и выполнит все автоматически.

### Вариант 3: Вручную через скрипты

1. **Экспорт:**
   ```bash
   export SUPABASE_DB_PASSWORD='your-password'
   ./scripts/export-supabase-data.sh
   ```

2. **Импорт:**
   ```bash
   export TIMEWEB_DB_PASSWORD='your-password'
   ./scripts/import-to-timeweb.sh supabase_backup_YYYYMMDD_HHMMSS.sql
   ```

## 🔧 После импорта данных

### Применение миграций

1. Подключитесь к Timeweb через веб-интерфейс
2. Откройте SQL Editor
3. Примените миграции в порядке:
   - `supabase/migrations/20250131000002_create_auth_tables.sql`

### Проверка

```sql
-- Проверка таблиц
\dt

-- Проверка данных
SELECT COUNT(*) FROM profiles;
SELECT COUNT(*) FROM rides;
```

## ✅ Готово!

После миграции:
1. Настройте backend сервер (см. `server/`)
2. Обновите Auth систему
3. Настройте Realtime
