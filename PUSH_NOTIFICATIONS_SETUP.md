# 🔔 Настройка Push-уведомлений

Push-уведомления реализованы с использованием Web Push API и Service Worker.

## 📋 Что реализовано:

1. ✅ **Service Worker** - обновлен для обработки push-уведомлений
2. ✅ **Хук useNotifications** - для работы с уведомлениями
3. ✅ **Компонент NotificationSettings** - настройки в профиле
4. ✅ **Таблица push_subscriptions** - для хранения подписок в БД
5. ✅ **Автоматическая регистрация** - при включении уведомлений

## 🔑 Настройка VAPID ключей (опционально)

Для полноценной работы push-уведомлений нужны VAPID ключи. Это опционально - уведомления будут работать и без них (локальные уведомления).

### Генерация VAPID ключей:

1. Установите `web-push`:
```bash
npm install -g web-push
```

2. Сгенерируйте ключи:
```bash
web-push generate-vapid-keys
```

3. Вы получите:
   - **Public Key** (публичный ключ)
   - **Private Key** (приватный ключ - НЕ публикуйте!)

### Добавление в переменные окружения:

**В Vercel:**
- **Key:** `VITE_VAPID_PUBLIC_KEY`
- **Value:** ваш публичный VAPID ключ

**В локальном .env:**
```env
VITE_VAPID_PUBLIC_KEY=ваш-публичный-ключ
```

## 🚀 Как это работает:

### 1. Пользователь включает уведомления:
- Заходит в профиль → раздел "Push-уведомления"
- Включает переключатель
- Браузер запрашивает разрешение
- Создается подписка и сохраняется в БД

### 2. Отправка уведомлений:

Уведомления можно отправлять через:
- **Supabase Edge Functions** (рекомендуется)
- **Внешний сервер** с web-push библиотекой
- **Supabase Database Triggers** + Edge Function

### Пример отправки через Edge Function:

```typescript
// supabase/functions/send-notification/index.ts
import { serve } from "https://deno.land/std@0.168.0/http/server.ts"
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

serve(async (req) => {
  const { userId, title, body, url } = await req.json()
  
  // Получаем подписку пользователя из БД
  const { data: subscription } = await supabase
    .from('push_subscriptions')
    .select('subscription')
    .eq('user_id', userId)
    .single()
  
  // Отправляем уведомление через web-push
  // (требует настройки на сервере)
  
  return new Response(JSON.stringify({ success: true }))
})
```

## 📱 Типы уведомлений:

### 1. Новые сообщения в чате
- Когда: новое сообщение в чате поездки
- Кому: водитель и забронировавшие пассажиры

### 2. Новые бронирования
- Когда: пассажир забронировал поездку
- Кому: водитель

### 3. Подтверждение бронирования
- Когда: водитель подтвердил бронирование
- Кому: пассажир

### 4. Отмена поездки
- Когда: поездка отменена
- Кому: все участники

## 🔧 Интеграция с существующим кодом:

### Отправка уведомления при новом сообщении:

```typescript
// В useMessages.ts или при отправке сообщения
import { sendPushNotification } from '@/lib/notifications';

// После успешной отправки сообщения
await sendPushNotification({
  userId: recipientId,
  title: 'Новое сообщение',
  body: `${senderName}: ${messageContent}`,
  url: `/ride/${rideId}`,
  tag: `message-${rideId}`,
});
```

### Отправка уведомления при бронировании:

```typescript
// В useBookings.ts после создания бронирования
await sendPushNotification({
  userId: ride.driver_id,
  title: 'Новое бронирование',
  body: `${passengerName} забронировал ${seats} мест`,
  url: `/my-rides`,
  tag: `booking-${bookingId}`,
});
```

## ⚠️ Важно:

1. **VAPID ключи опциональны** - уведомления работают и без них (локальные)
2. **HTTPS обязателен** - push-уведомления работают только на HTTPS
3. **Поддержка браузеров** - не все браузеры поддерживают (Chrome, Firefox, Edge - да)
4. **Разрешения** - пользователь должен разрешить уведомления

## 📚 Документация:

- [Web Push API](https://developer.mozilla.org/en-US/docs/Web/API/Push_API)
- [Service Worker API](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)
- [web-push библиотека](https://github.com/web-push-libs/web-push)

---

**Готово!** Push-уведомления настроены. Пользователи могут включить их в профиле! 🎉

